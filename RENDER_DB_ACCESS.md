# 🔧 Настройка доступа к Render PostgreSQL

## ❌ Проблема: Таймаут подключения

Ошибка `timeout expired` означает, что подключение к базе данных не может быть установлено. Это часто происходит, когда Render PostgreSQL не разрешает внешние подключения.

## ✅ Решение

### Вариант 1: Включить внешний доступ на Render

1. Зайдите в **Render Dashboard**
2. Выберите ваш **PostgreSQL сервис**
3. Перейдите в раздел **"Settings"** или **"Network"**
4. Найдите опцию **"Allow External Connections"** или **"Public Networking"**
5. **Включите** эту опцию
6. Если есть **IP Whitelist**, добавьте ваш текущий IP адрес

### Вариант 2: Использовать для локальной разработки

Для локальной разработки на вашем компьютере:
- Используйте **External Database URL** (который мы уже настроили)
- Убедитесь, что внешний доступ включен на Render

### Вариант 3: Проверить настройки файрвола

1. Убедитесь, что порт `5432` не заблокирован файрволом
2. Проверьте, что ваш интернет-провайдер не блокирует подключения

## 📝 Проверка настроек на Render

В Render Dashboard проверьте:

1. **Database Settings:**
   - ✅ External Connections: **Enabled**
   - ✅ Public Networking: **Enabled** (если есть)

2. **Network:**
   - ✅ Ваш IP добавлен в whitelist (если требуется)

3. **Connection Info:**
   - Используйте **External Database URL** для локальной разработки
   - Используйте **Internal Database URL** только для сервисов на Render

## 🧪 После включения доступа

После того как включите внешний доступ на Render, попробуйте снова:

```bash
cd backend
node test-connection-direct.js
```

Затем создайте тестового пользователя:

```bash
node create-test-user-yookassa.js
```

## 💡 Альтернативный вариант

Если внешний доступ недоступен, можно:
1. Развернуть backend на Render (тогда он сможет использовать Internal URL)
2. Использовать VPN или туннель
3. Использовать локальную БД для разработки


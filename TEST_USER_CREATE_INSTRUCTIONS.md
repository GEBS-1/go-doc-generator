# 🔧 Создание тестового пользователя

## ❌ Текущая проблема

При попытке входа получаете ошибку:
> "Тестовый пользователь не найден. Запустите скрипт create-test-user-yookassa.js"

При попытке создать через API получаем:
> 401 Unauthorized - Неверный секретный ключ

## ✅ Решение

### Вариант 1: Узнать правильный ADMIN_SECRET

1. Зайдите в **Render Dashboard**
2. Откройте ваш **Backend сервис**
3. Перейдите в **Environment** → **Environment Variables**
4. Найдите переменную `ADMIN_SECRET`
5. Скопируйте её значение

### Вариант 2: Создать через скрипт с правильным секретом

После того, как узнаете правильный ADMIN_SECRET, выполните:

```bash
cd backend
node test-create-user.js https://api.prepromo.online ВАШ-ПРАВИЛЬНЫЙ-ADMIN-SECRET
```

### Вариант 3: Через онлайн сервис

1. Откройте https://reqbin.com/curl или https://hoppscotch.io/
2. Method: **POST**
3. URL: `https://api.prepromo.online/api/admin/create-test-user`
4. Headers: `Content-Type: application/json`
5. Body:
   ```json
   {
     "secret": "ваш-admin-secret-из-render"
   }
   ```
6. Нажмите "Send"

## 📋 После создания

После успешного создания вы получите:
- ✅ Логин: `yookassa_test`
- ✅ Пароль: `YooKassa2025!Test`

Тогда можно будет войти через:
- URL: `https://prepromo.online/#/auth/test-login`

## 💡 Если не знаете ADMIN_SECRET

Если вы забыли, какой ADMIN_SECRET установлен на Render, можно:
1. Установить новый в Render Dashboard
2. Или использовать любой секрет, который вы помните
3. Важно, чтобы он совпадал с тем, что в запросе


# 📊 Реализация таблиц и графиков - План

## ✅ Что уже сделано:

1. **Функции генерации в `gigachat.ts`:**
   - `generateTable()` - генерация таблиц через GigaChat
   - `generateChartData()` - генерация данных для графиков
   - Типы: `TableData`, `ChartData`, `TableType`

2. **Утилита для графиков в `chartUtils.ts`:**
   - `chartToImage()` - конвертация графика в base64 изображение

3. **Обновлены интерфейсы:**
   - `Section` теперь включает `tables?: TableData[]` и `charts?: ChartData[]`

## 🔄 Что нужно доделать:

### 1. **Обновить DocumentEditor.tsx:**
   - Добавить функции генерации таблиц/графиков
   - Добавить UI для отображения таблиц
   - Добавить кнопки "Добавить таблицу/график"
   - Добавить возможность редактирования и удаления

### 2. **Обновить TitlePage.tsx:**
   - Добавить экспорт таблиц в DOCX (используя `Table`, `TableRow`, `TableCell` из `docx`)
   - Добавить экспорт графиков как изображений в DOCX

### 3. **Интегрировать в workflow:**
   - Автоматически предлагать таблицы/графики после генерации текста
   - Или добавлять по запросу пользователя

## 📝 Следующие шаги:

1. Дополнить DocumentEditor функциями генерации таблиц/графиков
2. Добавить экспорт в DOCX в TitlePage.tsx
3. Протестировать генерацию и экспорт

---

**Статус:** В процессе реализации


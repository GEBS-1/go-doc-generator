# 🔧 Настройка базы данных

## Текущая ситуация

База данных **недоступна**. Ошибка: база данных не найдена.

## Решение проблемы

### Вариант 1: Если PostgreSQL не запущен

1. **Запустите PostgreSQL сервер:**
   - Windows: Через Services (службы) или командой
   - Linux/Mac: `sudo service postgresql start` или `brew services start postgresql`

2. **Проверьте, что PostgreSQL запущен:**
   ```bash
   # Windows (PowerShell)
   Get-Service -Name postgresql*
   
   # Linux/Mac
   sudo service postgresql status
   ```

### Вариант 2: Если база данных не создана

1. **Подключитесь к PostgreSQL:**
   ```bash
   # Windows
   psql -U postgres
   
   # Или через pgAdmin
   ```

2. **Создайте базу данных:**
   ```sql
   CREATE DATABASE docugen;
   ```

3. **Создайте пользователя (если нужно):**
   ```sql
   CREATE USER Telebot WITH PASSWORD '0101';
   GRANT ALL PRIVILEGES ON DATABASE docugen TO Telebot;
   ```

4. **Подключитесь к новой базе:**
   ```sql
   \c docugen
   ```

### Вариант 3: Проверьте DATABASE_URL

Убедитесь, что в `backend/.env` правильно указан `DATABASE_URL`:

```env
DATABASE_URL=postgresql://Telebot:0101@127.0.0.1:5432/docugen
```

Формат:
```
postgresql://[пользователь]:[пароль]@[хост]:[порт]/[база_данных]
```

### Вариант 4: Используйте существующую базу данных

Если у вас уже есть база данных, просто обновите `DATABASE_URL` в `.env` файле.

## После настройки БД

1. **Проверьте подключение:**
   ```bash
   cd backend
   node test-db-connection.js
   ```

2. **Создайте тестового пользователя:**
   ```bash
   node create-test-user-yookassa.js
   ```

## Проверка состояния БД

Вы можете проверить состояние подключения к БД:

```bash
cd backend
node test-db-connection.js
```

Скрипт покажет:
- ✅ Есть ли DATABASE_URL
- ✅ Успешно ли подключение
- ❌ Какая ошибка, если подключение не удалось

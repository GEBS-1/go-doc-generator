# Инструкция по деплою на Render

## 📋 Подготовка

### 1. Убедитесь, что код закоммичен и запушен в Git
```bash
git add .
git commit -m "Prepare for deployment"
git push
```

---

## 🗄️ Шаг 1: Создание PostgreSQL на Render

1. Зайдите на [render.com](https://render.com)
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Заполните:
   - **Name**: `docugen-postgres` (или любое другое имя)
   - **Database**: `docugen` (или оставьте по умолчанию)
   - **User**: оставьте по умолчанию
   - **Region**: выберите ближайший (например, Oregon)
   - **Plan**: Free (для тестирования) или Basic/Pro (для продакшена)
4. Нажмите **"Create Database"**
5. **ВАЖНО**: После создания скопируйте:
   - **Internal Database URL** (для использования внутри Render)
   - **External Database URL** (для локального подключения, если нужно)

---

## 🔧 Шаг 2: Создание Backend сервиса на Render

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Заполните:
   - **Name**: `docugen-backend` (или любое другое имя)
   - **Region**: тот же, что и для PostgreSQL
   - **Branch**: `main` (или ваша основная ветка)
   - **Root Directory**: `backend`
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`

### Переменные окружения для Backend:

Добавьте в **Environment Variables**:

```env
# База данных
DATABASE_URL=<Internal Database URL из PostgreSQL>
PGSSLMODE=require

# JWT
AUTH_JWT_SECRET=<случайная_строка_минимум_32_символа>

# Telegram Bot
TELEGRAM_BOT_TOKEN=<ваш_токен_бота>
TELEGRAM_BOT_USERNAME=<username_бота_без_@>
FRONTEND_URL=https://ваш-фронт.onrender.com

# GigaChat API
GIGACHAT_CLIENT_ID=<ваш_client_id>
GIGACHAT_CLIENT_SECRET=<ваш_client_secret>
GIGACHAT_AUTH_KEY=<ваш_auth_key>
GIGACHAT_ALLOW_INSECURE_SSL=false

# YooKassa (опционально, если еще не настроено)
YOOKASSA_SHOP_ID=<ваш_shop_id>
YOOKASSA_SECRET_KEY=<ваш_секретный_ключ>
PAYMENT_RETURN_URL=https://ваш-фронт.onrender.com/payment/success
PAYMENT_FAIL_URL=https://ваш-фронт.onrender.com/payment/failed

# CORS
ALLOWED_ORIGINS=https://ваш-фронт.onrender.com

# Server
HOST=0.0.0.0
PORT=10000
NODE_VERSION=18.x
```

**Важно:**
- `DATABASE_URL` должен быть **Internal Database URL** (начинается с `postgres://...`)
- `AUTH_JWT_SECRET` - сгенерируйте случайную строку (можно использовать: `openssl rand -hex 32`)
- `FRONTEND_URL` - будет известен после создания фронтенда, обновите потом

4. Нажмите **"Create Web Service"**

---

## 🎨 Шаг 3: Создание Frontend статического сайта на Render

1. Нажмите **"New +"** → **"Static Site"**
2. Подключите ваш GitHub репозиторий
3. Заполните:
   - **Name**: `docugen-frontend` (или любое другое имя)
   - **Branch**: `main` (или ваша основная ветка)
   - **Root Directory**: `.` (корень проекта)
   - **Build Command**: `npm install && npm run build`
   - **Publish Directory**: `dist`

### Переменные окружения для Frontend:

Добавьте в **Environment Variables**:

```env
VITE_BACKEND_URL=https://ваш-backend.onrender.com
VITE_REQUIRE_AUTH=true
VITE_TELEGRAM_BOT_USERNAME=<username_бота_без_@>
```

**Важно:**
- `VITE_BACKEND_URL` - URL вашего backend сервиса (например: `https://docugen-backend.onrender.com`)
- Обновите `FRONTEND_URL` в backend после создания фронтенда

4. Нажмите **"Create Static Site"**

---

## 🔄 Шаг 4: Обновление переменных окружения

### После создания Frontend:

1. Скопируйте URL фронтенда (например: `https://docugen-frontend.onrender.com`)
2. Зайдите в Backend сервис → **Environment**
3. Обновите:
   ```
   FRONTEND_URL=https://docugen-frontend.onrender.com
   ALLOWED_ORIGINS=https://docugen-frontend.onrender.com
   ```
4. Нажмите **"Save Changes"** → Backend автоматически перезапустится

### После создания Backend:

1. Скопируйте URL бэкенда (например: `https://docugen-backend.onrender.com`)
2. Зайдите в Frontend сервис → **Environment**
3. Обновите:
   ```
   VITE_BACKEND_URL=https://docugen-backend.onrender.com
   ```
4. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**

---

## 🤖 Шаг 5: Настройка Telegram Bot Webhook

После деплоя backend нужно настроить webhook для Telegram бота:

1. Узнайте URL вашего backend: `https://ваш-backend.onrender.com`
2. Установите webhook через Telegram Bot API:
   ```bash
   curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook" \
     -d "url=https://ваш-backend.onrender.com/api/telegram/webhook"
   ```

   Или используйте браузер:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/setWebhook?url=https://ваш-backend.onrender.com/api/telegram/webhook
   ```

3. Проверьте webhook:
   ```bash
   curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getWebhookInfo"
   ```

**Альтернатива:** Если не хотите использовать webhook, бот будет работать через polling (уже настроено в коде).

---

## 🔍 Шаг 6: Проверка работы

### 1. Проверка Backend:
```bash
curl https://ваш-backend.onrender.com/health
```
Должен вернуть: `{"status":"ok",...}`

### 2. Проверка Frontend:
- Откройте `https://ваш-фронт.onrender.com`
- Должна загрузиться главная страница

### 3. Проверка авторизации:
1. Откройте фронтенд
2. Нажмите "Войти"
3. Авторизуйтесь через Telegram Login Widget
4. Должен появиться ваш профиль

### 4. Проверка Telegram бота:
1. Найдите вашего бота в Telegram
2. Нажмите `/start`
3. Должно прийти приветственное сообщение

### 5. Проверка базы данных:
- Зайдите в PostgreSQL на Render → **"Connect"** → **"psql"**
- Выполните:
  ```sql
  \dt                    -- Список таблиц
  SELECT * FROM users;   -- Проверка пользователей
  ```

---

## 🐛 Решение проблем

### Backend не запускается:
1. Проверьте логи в Render → Backend → **"Logs"**
2. Убедитесь, что все переменные окружения заполнены
3. Проверьте, что `DATABASE_URL` правильный

### Frontend не собирается:
1. Проверьте логи в Render → Frontend → **"Logs"**
2. Убедитесь, что `VITE_BACKEND_URL` правильный
3. Проверьте, что все зависимости установлены

### Telegram бот не отвечает:
1. Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
2. Проверьте логи backend на наличие ошибок
3. Убедитесь, что webhook установлен (если используете)

### База данных не подключается:
1. Убедитесь, что используете **Internal Database URL** (не External)
2. Проверьте, что `PGSSLMODE=require` установлен
3. Проверьте логи backend

---

## 📝 Чеклист перед деплоем

- [ ] Код закоммичен и запушен в Git
- [ ] PostgreSQL создан на Render
- [ ] Backend сервис создан с правильными переменными
- [ ] Frontend сервис создан с правильными переменными
- [ ] `FRONTEND_URL` обновлен в backend
- [ ] `VITE_BACKEND_URL` обновлен во frontend
- [ ] Webhook установлен для Telegram бота (опционально)
- [ ] Проверена работа авторизации
- [ ] Проверена работа Telegram бота
- [ ] Проверена работа базы данных

---

## 🚀 После деплоя

1. **Настройте домен** (если нужно) - см. `DEPLOYMENT_CHECKLIST.md`
2. **Добавьте фавикон** (если нужно) - см. `DEPLOYMENT_CHECKLIST.md`
3. **Настройте YooKassa** (если нужно) - см. `DEPLOYMENT_CHECKLIST.md`

---

## 📞 Полезные ссылки

- [Render Dashboard](https://dashboard.render.com)
- [Render Docs](https://render.com/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)


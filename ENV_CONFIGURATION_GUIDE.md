# ⚙️ Настройка .env файла для YooKassa

## 📋 Что нужно настроить в `backend/.env`

### ✅ Обязательно для YooKassa:

```env
# YooKassa ключи (уже добавили)
YOOKASSA_SHOP_ID=123456
YOOKASSA_SECRET_KEY=test_MjIyNjMyNzQzNC0xLVBTVS1OdmVjT...

# Для локального тестирования (ОБЯЗАТЕЛЬНО раскомментируйте и установите):
FRONTEND_URL=http://localhost:8080
PAYMENT_RETURN_URL=http://localhost:8080/payment/success
PAYMENT_FAIL_URL=http://localhost:8080/payment/failed
```

---

## 🔍 Что означают эти переменные:

### `FRONTEND_URL`
- **Назначение**: Базовый URL вашего фронтенда
- **Используется для**: 
  - Ссылок в сообщениях Telegram
  - Определения CORS разрешенных источников
- **Для локального тестирования**: `http://localhost:8080`
- **Для продакшн**: `https://yourdomain.com`

### `PAYMENT_RETURN_URL`
- **Назначение**: URL, куда YooKassa перенаправит после успешной оплаты
- **Используется в**: Создании платежа через `checkout.createPayment()`
- **Для локального тестирования**: `http://localhost:8080/payment/success`
- **Для продакшн**: `https://yourdomain.com/payment/success`

### `PAYMENT_FAIL_URL`
- **Назначение**: URL, куда перенаправить при ошибке/отмене оплаты
- **Для локального тестирования**: `http://localhost:8080/payment/failed`
- **Для продакшн**: `https://yourdomain.com/payment/failed`

---

## 📝 Пример полного `.env` для локального тестирования:

```env
# YooKassa
YOOKASSA_SHOP_ID=123456
YOOKASSA_SECRET_KEY=test_ваш_ключ

# Frontend URLs (для локального тестирования)
FRONTEND_URL=http://localhost:8080
PAYMENT_RETURN_URL=http://localhost:8080/payment/success
PAYMENT_FAIL_URL=http://localhost:8080/payment/failed

# Остальные настройки...
AUTH_JWT_SECRET=your-secret-key
TELEGRAM_BOT_TOKEN=your-bot-token
```

---

## ⚠️ Про строки 21-23 (Telegram бот):

Строки 21-23 в `env.example` это:
```env
# Telegram authorization (имя бота без @)
# VITE_TELEGRAM_BOT_USERNAME=your_bot_name
```

**Что делать:**
- Если используете Telegram авторизацию → раскомментируйте и укажите имя бота
- Если не используете → можно оставить закомментированным

**Это НЕ влияет на работу YooKassa**, но нужно для Telegram авторизации на сайте.

---

## ✅ Чек-лист настройки:

- [x] `YOOKASSA_SHOP_ID` - добавлен
- [x] `YOOKASSA_SECRET_KEY` - добавлен
- [ ] `FRONTEND_URL` - нужно добавить/раскомментировать
- [ ] `PAYMENT_RETURN_URL` - нужно добавить/раскомментировать
- [ ] `PAYMENT_FAIL_URL` - нужно добавить/раскомментировать (опционально)

---

## 🚀 После настройки:

1. Сохраните файл `.env`
2. Перезапустите backend сервер:
   ```bash
   cd backend
   npm run dev
   ```
3. Проверьте, что нет предупреждений в консоли

---

## 🔄 Для продакшн (когда будете деплоить):

Измените на реальные домены:
```env
FRONTEND_URL=https://yourdomain.com
PAYMENT_RETURN_URL=https://yourdomain.com/payment/success
PAYMENT_FAIL_URL=https://yourdomain.com/payment/failed
```


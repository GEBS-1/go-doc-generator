# 🔧 Настройка DATABASE_URL - Пошаговая инструкция

## ✅ У вас есть 3 способа настроить DATABASE_URL

---

## 🚀 Способ 1: Автоматическая настройка (РЕКОМЕНДУЕТСЯ)

Запустите интерактивный помощник:

```bash
node backend/setup-env.js
```

Скрипт попросит ввести:
- Имя пользователя PostgreSQL
- Пароль
- Хост (обычно `localhost`)
- Порт (обычно `5432`)
- Имя базы данных (например, `godocgenerator`)

**Преимущества:**
- ✅ Автоматически проверит подключение
- ✅ Создаст/обновит `.env` файл
- ✅ Сгенерирует безопасный JWT секрет

---

## 🔍 Способ 2: Проверка текущего подключения

Если `DATABASE_URL` уже установлен в `backend/.env`, проверьте подключение:

```bash
node backend/test-db-connection.js
```

Скрипт покажет:
- ✅ Успешное подключение + версию PostgreSQL
- ❌ Ошибки подключения с рекомендациями

---

## 📝 Способ 3: Ручная настройка

### Шаг 1: Откройте файл `backend/.env`

Если файла нет - создайте его или скопируйте из шаблона.

### Шаг 2: Найдите/добавьте строку `DATABASE_URL`

Формат:
```
DATABASE_URL=postgres://username:password@host:port/database
```

### Примеры:

**Локальный PostgreSQL:**
```env
DATABASE_URL=postgres://postgres:ваш_пароль@localhost:5432/godocgenerator
```

**Облачный сервис (Render, Supabase, Neon):**
```env
DATABASE_URL=postgres://user:pass@host.provider.com:5432/dbname
PGSSLMODE=require
```

### Шаг 3: Сохраните файл

### Шаг 4: Проверьте подключение

```bash
node backend/test-db-connection.js
```

---

## 💡 Где взять DATABASE_URL?

### Если PostgreSQL установлен локально:

1. **Windows:**
   - Обычно: `postgres://postgres:ваш_пароль@localhost:5432/имя_базы`
   - Пароль задавался при установке PostgreSQL

2. **Проверка через psql:**
   ```bash
   psql -U postgres -h localhost
   ```

### Если используете облачный сервис:

- **Render.com:** Dashboard → PostgreSQL → "Internal Database URL"
- **Supabase:** Project Settings → Database → Connection string
- **Neon.tech:** Dashboard → Connection Details
- **Railway:** Variables → DATABASE_URL

---

## 🆘 Частые проблемы

### ❌ "DATABASE_URL не задан"

**Решение:** Добавьте `DATABASE_URL` в `backend/.env`

### ❌ "connection refused"

**Решение:**
- Проверьте, что PostgreSQL запущен
- Проверьте хост и порт

### ❌ "password authentication failed"

**Решение:**
- Проверьте правильность пароля
- Убедитесь, что пользователь существует

### ❌ "database does not exist"

**Решение:** Создайте базу данных:
```sql
CREATE DATABASE godocgenerator;
```

---

## ✅ После настройки

1. **Проверьте подключение:**
   ```bash
   node backend/test-db-connection.js
   ```

2. **Создайте тестового пользователя:**
   ```bash
   node backend/create-test-user.js
   ```

3. **Запустите backend:**
   ```bash
   cd backend
   npm run dev
   ```

4. **Таблицы создадутся автоматически** при первом запуске backend

---

## 📖 Подробная документация

Смотрите файл `DATABASE_SETUP.md` для детальной информации.

---

## 🎯 Быстрый старт

```bash
# 1. Автоматическая настройка
node backend/setup-env.js

# 2. Проверка
node backend/test-db-connection.js

# 3. Создание тестового пользователя
node backend/create-test-user.js

# 4. Запуск
cd backend && npm run dev
```


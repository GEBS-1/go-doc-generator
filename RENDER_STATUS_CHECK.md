# Проверка статуса деплоя на Render

## 🔍 Что нужно проверить

### 1. Зайдите на Render Dashboard
- URL: https://dashboard.render.com
- Войдите в свой аккаунт

### 2. Проверьте список сервисов

Посмотрите, какие сервисы уже созданы:

#### ✅ Backend (Web Service)
- **Название**: `docugen-backend` или похожее
- **Тип**: Web Service
- **Статус**: Live / Building / Failed
- **URL**: `https://xxx.onrender.com`

#### ✅ PostgreSQL
- **Название**: `docugen-postgres` или похожее
- **Тип**: PostgreSQL
- **Статус**: Available
- **Internal Database URL**: `postgres://...`

#### ✅ Frontend (Static Site)
- **Название**: `docugen-frontend` или похожее
- **Тип**: Static Site
- **Статус**: Live / Building / Failed
- **URL**: `https://xxx.onrender.com`

---

## 📋 Что делать в зависимости от статуса

### Сценарий 1: Backend УЖЕ создан ✅

**Что проверить:**
1. Зайдите в Backend сервис → **Environment**
2. Проверьте переменные окружения:
   - `DATABASE_URL` - должен быть Internal URL из PostgreSQL
   - `AUTH_JWT_SECRET` - должен быть заполнен
   - `TELEGRAM_BOT_TOKEN` - должен быть заполнен
   - `FRONTEND_URL` - должен быть URL фронтенда (если фронтенд создан)

**Что обновить:**
- Если PostgreSQL создан, но `DATABASE_URL` не обновлен → обновите
- Если Frontend создан, но `FRONTEND_URL` не обновлен → обновите
- После обновления переменных → Backend автоматически перезапустится

### Сценарий 2: Backend НЕ создан ❌

**Что делать:**
1. Следуйте инструкциям в `RENDER_DEPLOYMENT.md`
2. Создайте Backend сервис
3. Добавьте все переменные окружения
4. Дождитесь успешного деплоя

### Сценарий 3: Частично создано ⚠️

**Если есть только PostgreSQL:**
- Создайте Backend сервис
- Используйте существующий PostgreSQL

**Если есть только Backend:**
- Проверьте переменные окружения
- Создайте PostgreSQL (если нужно)
- Создайте Frontend

---

## 🔧 Быстрая проверка через команды

### Проверка Backend (если URL известен):
```bash
curl https://ваш-backend.onrender.com/health
```

Должен вернуть: `{"status":"ok",...}`

### Проверка Frontend (если URL известен):
- Откройте в браузере: `https://ваш-фронт.onrender.com`
- Должна загрузиться главная страница

---

## 📝 Что сообщить

После проверки сообщите:
1. ✅/❌ Backend создан?
2. ✅/❌ PostgreSQL создан?
3. ✅/❌ Frontend создан?
4. Какие переменные окружения уже заполнены?
5. Какие URL у сервисов?

Это поможет понять, что нужно сделать дальше!


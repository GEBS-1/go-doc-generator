# 📄 Реализация этапа титульника

## 📋 Обзор

Этап титульника (`TitlePage`) — финальный этап генерации документа, где пользователь:
1. Заполняет данные для титульного листа (тема, автор, организация и т.д.)
2. Может загрузить свой шаблон DOCX или использовать шаблон по умолчанию
3. Генерирует и скачивает готовый документ в формате .docx

---

## 🏗️ Архитектура

### Основные файлы

1. **`src/components/steps/TitlePage.tsx`** (1505 строк)
   - Главный компонент этапа титульника
   - Форма для ввода данных
   - Предпросмотр титульного листа
   - Генерация документа

2. **`src/lib/titleTemplate.ts`** (440 строк)
   - Рендеринг титульного листа
   - Работа с DOCX шаблонами
   - Конвертация шаблонов в Paragraph[]

3. **`src/lib/gigachat.ts`**
   - Генерация контента через GigaChat API
   - Обработка ошибок API

4. **`src/lib/api.ts`**
   - API запросы к backend
   - Проверка квот документов

---

## 🔄 Поток работы

### 1. Инициализация компонента

```typescript
export const TitlePage = ({ sections, theme, onBack }: TitlePageProps) => {
  const { token, refreshProfile } = useAuth();
  const authRequired = import.meta.env.VITE_REQUIRE_AUTH !== "false";
  
  // Состояние полей титульного листа
  const [titleFields, setTitleFields] = useState({
    theme: theme || "",
    documentType: "courseWork",
    year: new Date().getFullYear().toString(),
    university: defaultTitleFields.UNIVERSITY,
    // ... остальные поля
  });
  
  // Шаблон титульного листа (опционально)
  const [templateFile, setTemplateFile] = useState<File | null>(null);
};
```

**Что происходит:**
- Получает `theme` из предыдущего этапа
- Инициализирует поля значениями по умолчанию
- Проверяет авторизацию (если требуется)

---

### 2. Форма для ввода данных

**Структура формы:**

```typescript
<Card className="p-6 space-y-4">
  <h3>Основная информация</h3>
  
  {/* Тема документа (обязательное поле) */}
  <Input
    value={titleFields.theme}
    onChange={(e) => handleFieldChange("theme", e.target.value)}
    placeholder="Название работы"
  />
  
  {/* Тип документа (Реферат, Курсовая, ВКР и т.д.) */}
  <Select
    value={titleFields.documentType}
    onValueChange={(value) => handleFieldChange("documentType", value)}
  >
    {/* Опции: essay, courseWork, diploma, article, report */}
  </Select>
  
  {/* Год */}
  <Input value={titleFields.year} />
</Card>

<Card>
  <h3>Учебное заведение</h3>
  {/* Организация, Факультет, Кафедра, Направление, Профиль, Город */}
</Card>

<Card>
  <h3>Автор и научный руководитель</h3>
  {/* Автор (обязательное), Группа, Руководитель, Должность руководителя */}
</Card>

<Card>
  <h3>Индивидуальный шаблон</h3>
  {/* Загрузка DOCX шаблона (опционально) */}
  <Input type="file" accept=".docx" />
</Card>
```

**Поля формы:**
- **Обязательные:** `theme`, `author`
- **Опциональные:** Все остальные (есть значения по умолчанию)

---

### 3. Предпросмотр титульного листа

**Компонент предпросмотра:**

```typescript
<Card className="p-8 bg-secondary/30 sticky top-24">
  <h3>Предпросмотр</h3>
  <div className="space-y-8 text-center border-2 border-dashed">
    {/* Организация */}
    <p>{titleFields.university || defaultTitleFields.UNIVERSITY}</p>
    
    {/* Тип документа и тема */}
    <p className="text-xs uppercase">{selectedDocType.label}</p>
    <p className="text-lg font-bold">«{titleFields.theme}»</p>
    
    {/* Автор, группа, руководитель */}
    <p>Автор: {titleFields.author || "ФИО автора"}</p>
    
    {/* Город и год */}
    <p>{titleFields.city}, {titleFields.year}</p>
  </div>
</Card>
```

**Что показывает:**
- Предпросмотр в реальном времени
- Использует значения из формы или по умолчанию
- Стилизован как титульный лист

---

### 4. Генерация документа

**Основная функция:** `generateDocxDocument()`

#### 4.1. Валидация и проверка квот

```typescript
// Проверка обязательных полей
if (!titleFields.theme.trim() || !titleFields.author.trim()) {
  toast.error("Заполните тему и автора");
  return;
}

// Проверка авторизации
if (authRequired && !token) {
  toast.error("Не удалось подтвердить авторизацию. Войдите снова.");
  return;
}

// Проверка квоты документов (если требуется авторизация)
if (quotaEnabled) {
  const response = await apiFetch<ConsumeResponse>("/api/subscription/consume", {
    method: "POST",
    token,
    body: JSON.stringify({ consume: false }), // Только проверка, не списание
  });
  
  if (!response.allowed) {
    toast.error("Превышен лимит документов");
    return;
  }
}
```

#### 4.2. Подготовка данных титульного листа

```typescript
const templateData: TitleTemplateData = {
  THEME: titleFields.theme.trim(),
  DOC_TYPE: docTypeConfig.templateValue, // "КУРСОВАЯ РАБОТА"
  YEAR: normalizedYear,
  UNIVERSITY: titleFields.university?.trim() || defaultTitleFields.UNIVERSITY,
  FACULTY: titleFields.faculty?.trim() || defaultTitleFields.FACULTY,
  // ... остальные поля
  
  // Составные поля для шаблона
  AUTHOR_LINE: `Выполнил: ${titleFields.author.trim()}`,
  GROUP_LINE: titleFields.group?.trim() ? `Группа: ${titleFields.group.trim()}` : "",
  SUPERVISOR_LINE: titleFields.supervisor?.trim()
    ? `Руководитель: ${titleFields.supervisor.trim()}`
    : "",
  LOCATION_LINE: [normalizedCity, normalizedYear].filter(Boolean).join(", "),
};
```

#### 4.3. Рендеринг титульного листа

**Два варианта:**

**Вариант 1: Загруженный пользователем шаблон**

```typescript
if (templateFile) {
  try {
    const buffer = await templateFile.arrayBuffer();
    titlePageContent = await renderTitleTemplate(templateData, buffer);
  } catch (templateError) {
    // Если шаблон невалидный — используем шаблон по умолчанию
    console.error("Title template render error", templateError);
    toast.error("Не удалось применить загруженный шаблон");
    titlePageContent = buildFallbackTitlePage(templateData);
  }
}
```

**Вариант 2: Шаблон по умолчанию**

```typescript
else {
  titlePageContent = buildFallbackTitlePage(templateData);
}
```

**Что делает `buildFallbackTitlePage()`:**
- Создает титульный лист программно через `docx` библиотеку
- Использует стили по ГОСТ (Times New Roman, выравнивание, отступы)
- Возвращает массив `Paragraph[]`

---

### 5. Структура титульного листа (по умолчанию)

**Порядок элементов:**

```typescript
// 1. Министерство (по центру, жирный)
"МИНИСТЕРСТВО НАУКИ И ВЫСШЕГО ОБРАЗОВАНИЯ"

// 2. Страна (по центру, жирный)
"РОССИЙСКОЙ ФЕДЕРАЦИИ"

// 3. Организация (по центру)
{{UNIVERSITY}}

// 4. Факультет (по центру)
{{FACULTY}}

// 5. Кафедра (по центру)
{{DEPARTMENT}}

// 6. Направление подготовки (по центру)
{{DIRECTION}}

// 7. Профиль (по центру)
{{PROFILE}}

// 8. Тип документа (по центру, жирный)
{{DOC_TYPE}}

// 9. Тема (по центру, жирный, в кавычках)
«{{THEME}}»

// 10. Автор (слева)
Выполнил: {{AUTHOR}}

// 11. Группа (слева)
Группа: {{GROUP}}

// 12. Руководитель (слева)
Руководитель: {{SUPERVISOR}}

// 13. Должность руководителя (слева)
{{SUPERVISOR_POSITION}}

// 14. Город и год (справа)
{{CITY}}, {{YEAR}}
```

**Стили по ГОСТ:**
- Шрифт: Times New Roman
- Размер: 12–14 пт (24–28 в units docx)
- Выравнивание: центр для заголовков, слева для автора, справа для даты
- Отступы: согласно ГОСТ

---

### 6. Работа с пользовательским шаблоном

**Как работает `renderTitleTemplate()`:**

```typescript
export const renderTitleTemplate = async (
  data: TitleTemplateData,
  templateBuffer?: ArrayBuffer,
): Promise<Paragraph[]> => {
  // 1. Получаем шаблон (пользовательский или по умолчанию)
  const buffer = templateBuffer ?? (await getDefaultTemplateBuffer());

  // 2. Открываем DOCX как ZIP
  const zip = new PizZip(buffer);
  
  // 3. Создаем Docxtemplater для замены плейсхолдеров
  const doc = new Docxtemplater(zip, {
    paragraphLoop: true,
    linebreaks: true,
  });

  // 4. Заменяем плейсхолдеры {{FIELD}} на значения
  doc.render(data);

  // 5. Генерируем новый DOCX
  const generatedBuffer = doc.getZip().generate({
    type: "arraybuffer",
  });

  // 6. Извлекаем document.xml из DOCX
  const renderedZip = await JSZip.loadAsync(generatedBuffer);
  const documentXml = await renderedZip.file("word/document.xml")?.async("string");

  // 7. Парсим XML и конвертируем в Paragraph[]
  return convertDocumentXmlToParagraphs(documentXml);
};
```

**Поддерживаемые плейсхолдеры:**
- `{{THEME}}` - Тема документа
- `{{DOC_TYPE}}` - Тип документа (КУРСОВАЯ РАБОТА)
- `{{YEAR}}` - Год
- `{{UNIVERSITY}}` - Организация
- `{{FACULTY}}` - Факультет
- `{{DEPARTMENT}}` - Кафедра
- `{{DIRECTION}}` - Направление подготовки
- `{{PROFILE}}` - Профиль
- `{{AUTHOR}}` - Автор
- `{{GROUP}}` - Группа
- `{{SUPERVISOR}}` - Руководитель
- `{{SUPERVISOR_POSITION}}` - Должность руководителя
- `{{CITY}}` - Город
- `{{AUTHOR_LINE}}` - Готовая строка "Выполнил: ..."
- `{{GROUP_LINE}}` - Готовая строка "Группа: ..."
- `{{SUPERVISOR_LINE}}` - Готовая строка "Руководитель: ..."
- `{{SUPERVISOR_POSITION_LINE}}` - Должность руководителя
- `{{DIRECTION_LINE}}` - Готовая строка "Направление подготовки: ..."
- `{{PROFILE_LINE}}` - Готовая строка "Профиль: ..."
- `{{LOCATION_LINE}}` - Готовая строка "Город, Год"

**Важно:**
- Каждый плейсхолдер должен быть **полностью** в одном параграфе
- Нельзя разбивать плейсхолдеры: `{{THE` + `ME}}` — это ошибка
- Используйте один раз каждый плейсхолдер (не дублируйте)

---

### 7. Генерация основного документа

**После титульного листа генерируется:**

1. **Содержание (оглавление)**
   ```typescript
   const tableOfContents: Paragraph[] = [
     new Paragraph({
       children: [new TextRun({ text: "Содержание", bold: true })],
       alignment: AlignmentType.CENTER,
     })
   ];
   
   sections.forEach((section) => {
     if (!isBibliography) {
       tableOfContents.push(
         new Paragraph({
           children: [new TextRun({ text: `${number}. ${section.title}` })]
         })
       );
     }
   });
   ```

2. **Основное содержимое разделов**
   - Заголовки разделов
   - Параграфы текста
   - Таблицы (если есть)
   - Графики (если есть)
   - Списки (маркированные и нумерованные)
   - Ссылки на источники

3. **Список литературы**
   - Автоматически извлекается из секции "Литература"
   - Нумеруется
   - Ссылки заменяются на номера

---

### 8. Формирование финального DOCX

**Структура документа:**

```typescript
const doc = new Document({
  creator: "DocuGen AI",
  title: titleFields.theme,
  description: `Документ на тему: ${theme}`,
  sections: [
    // 1. Титульный лист (без нумерации)
    {
      properties: {
        type: SectionType.NEXT_PAGE,
        page: { margin: { /* ГОСТ отступы */ } },
        // Нет нумерации
      },
      children: [...titlePageContent],
    },
    
    // 2. Содержание (нумерация с 2)
    {
      properties: {
        type: SectionType.NEXT_PAGE,
        pageNumberStart: 2,
      },
      headers: { default: new Header({ children: [] }) },
      footers: {
        default: new Footer({
          children: [new Paragraph({
            children: [new TextRun({ children: [PageNumber.CURRENT] })]
          })]
        })
      },
      children: [...tableOfContents],
    },
    
    // 3. Основное содержимое
    {
      properties: {
        page: { margin: { /* ГОСТ отступы */ } },
      },
      headers: { default: new Header({ children: [] }) },
      footers: { default: pageNumberFooter },
      children: [...documentSections],
    },
  ],
});
```

**Отступы по ГОСТ:**
- Левое поле: 3 см (8508 twips)
- Остальные поля: 1.5 см (850 twips)
- Верхнее и нижнее: 2 см (1134 twips)

**Нумерация:**
- Титульный лист: без номера
- Содержание: начинается с 2
- Основное содержимое: продолжает нумерацию

---

### 9. Сохранение и списание квоты

```typescript
// 1. Генерируем DOCX в Blob
const blob = await Packer.toBlob(doc);

// 2. Скачиваем файл
saveAs(blob, `${titleFields.theme || "document"}.docx`);

// 3. Списываем квоту (если требуется авторизация)
if (quotaEnabled) {
  try {
    const consumeResponse = await apiFetch<ConsumeResponse>(
      "/api/subscription/consume",
      {
        method: "POST",
        token,
        body: JSON.stringify({ consume: true }), // Списание
      }
    );
    
    await refreshProfile(); // Обновляем профиль пользователя
  } catch (consumeError) {
    console.error("Document quota consume error:", consumeError);
  }
}

// 4. Показываем успех
toast.success("Документ скачан успешно!", {
  description: formatUsageDescription(postConsumeUsage ?? preCheckUsage),
});
```

---

## 🛠️ Технические детали

### Библиотеки

1. **`docx`** (v9.5.1)
   - Создание DOCX документов
   - Работа с параграфами, таблицами, изображениями
   - Настройка стилей и форматирования

2. **`docxtemplater`** (v3.67.3)
   - Замена плейсхолдеров в DOCX шаблонах
   - Поддержка циклов и условий

3. **`pizzip`** (v3.2.0) + **`jszip`** (v3.10.1)
   - Работа с DOCX как с ZIP архивом
   - Извлечение и изменение XML внутри DOCX

4. **`file-saver`** (v2.0.5)
   - Сохранение файла на клиенте

---

### Обработка ошибок

**Ошибки валидации:**
- Не заполнены обязательные поля → `toast.error("Заполните тему и автора")`
- Нет авторизации → `toast.error("Не удалось подтвердить авторизацию")`

**Ошибки квот:**
- Превышен лимит → `toast.error("Превышен лимит документов")`
- Нет подписки → `toast.error("Не найдена активная подписка")`

**Ошибки шаблона:**
- Некорректный шаблон → `toast.error("Не удалось применить загруженный шаблон")` + fallback на шаблон по умолчанию

**Ошибки генерации:**
- Проблемы с памятью → `toast.error("Недостаточно памяти для создания документа")`
- Проблемы с сохранением → `toast.error("Проблема с сохранением файла")`

---

## 📊 Производительность

**Оптимизации:**
- Кэширование шаблона по умолчанию (`defaultTemplatePromise`)
- Ленивая генерация изображений графиков
- Обработка больших документов через стриминг (через Blob)

**Ограничения:**
- Размер документа ограничен памятью браузера
- Большие шаблоны могут замедлить рендеринг

---

## 🔐 Безопасность

**Проверки:**
- Валидация обязательных полей на клиенте и сервере
- Проверка авторизации перед генерацией
- Проверка квот перед списанием

**Защита от злоупотреблений:**
- Квоты на количество документов
- Проверка подписки перед генерацией
- Ограничения на размер файлов

---

## 🎨 Кастомизация

**Как добавить новые поля:**
1. Добавь поле в `titleFields` state
2. Добавь Input в форму
3. Добавь поле в `templateData`
4. Обнови `TitleTemplateData` интерфейс
5. Добавь плейсхолдер в шаблон

**Как изменить шаблон по умолчанию:**
1. Отредактируй `createDefaultTemplate()` в `src/lib/titleTemplate.ts`
2. Измени порядок параграфов или стили

**Как изменить стили:**
1. Измени размеры шрифтов (`size: 28` → другой размер)
2. Измени выравнивание (`alignment: AlignmentType.CENTER`)
3. Измени отступы (`spacing: { after: 200 }`)

---

## ✅ Готово!

Этап титульника полностью реализован и готов к использованию. Он поддерживает:
- ✅ Заполнение данных через форму
- ✅ Предпросмотр в реальном времени
- ✅ Загрузку пользовательских шаблонов
- ✅ Генерацию документов по ГОСТ
- ✅ Проверку квот и авторизации
- ✅ Обработку ошибок


# 🌐 Перенос домена с Nethouse на Render

## 📋 Пошаговая инструкция для Nethouse

### Шаг 1: Получи информацию о домене в Render

#### 1.1. Frontend (Static Site)

1. Зайди в Render Dashboard → **Static Site** (`go-doc-generator`)
2. Открой **Settings** → **Custom Domains**
3. Нажми **Add Custom Domain**
4. Введи свой домен (например: `example.com` или `www.example.com`)
5. **Запиши**, что Render покажет:
   - **CNAME target** (например: `go-doc-generator.onrender.com`)
   - Или **A record IP** (если потребуется)

#### 1.2. Backend (Web Service)

1. Зайди в Render Dashboard → **Web Service** (`go-doc-generator-backend`)
2. Открой **Settings** → **Custom Domains**
3. Нажми **Add Custom Domain**
4. Введи поддомен для backend (например: `api.example.com`)
5. **Запиши**:
   - **CNAME target** (например: `go-doc-generator-backend.onrender.com`)

---

### Шаг 2: Вход в панель Nethouse

#### 2.1. Зайди в личный кабинет Nethouse

1. Открой https://www.nethouse.ru/
2. Войди в **Личный кабинет**
3. Перейди в раздел **Домены** или **Управление доменами**

---

### Шаг 3: Настройка DNS записей на Nethouse

#### 3.1. Найди настройки DNS

1. В личном кабинете найди свой домен
2. Открой **Управление доменом** или **DNS настройки**
3. Перейди в раздел **DNS записи** или **Редактирование DNS**

#### 3.2. Удали старые DNS записи (если есть)

Если у тебя были A записи, которые указывали на Nethouse:
- Удали или измени их (они больше не нужны)

#### 3.3. Добавь CNAME записи для Frontend

**Вариант A: Если домен без www (example.com)**

Найди или создай CNAME запись:
```
Тип:     CNAME
Имя:     @
Значение: go-doc-generator.onrender.com
TTL:     3600 (или Auto)
```

**Вариант B: Если домен с www (www.example.com)**

Создай CNAME запись:
```
Тип:     CNAME
Имя:     www
Значение: go-doc-generator.onrender.com
TTL:     3600 (или Auto)
```

**Если нужно оба варианта (@ и www):**

1. Добавь CNAME для `@`:
   ```
   Тип:     CNAME
   Имя:     @
   Значение: go-doc-generator.onrender.com
   ```

2. Добавь CNAME для `www`:
   ```
   Тип:     CNAME
   Имя:     www
   Значение: go-doc-generator.onrender.com
   ```

**⚠️ ВАЖНО:** Если Nethouse не поддерживает CNAME для корневого домена (`@`), Render может потребовать A запись. В этом случае:
- Render покажет IP адрес
- Создай A запись вместо CNAME для `@`:
  ```
  Тип:     A
  Имя:     @
  Значение: <IP адрес от Render>
  TTL:     3600
  ```

#### 3.4. Добавь CNAME запись для Backend

Создай CNAME запись для поддомена backend:
```
Тип:     CNAME
Имя:     api
Значение: go-doc-generator-backend.onrender.com
TTL:     3600 (или Auto)
```

**Если хочешь другой поддомен** (например, `backend`):
```
Тип:     CNAME
Имя:     backend
Значение: go-doc-generator-backend.onrender.com
```

---

### Шаг 4: Сохрани изменения

1. Нажми **Сохранить** или **Применить** в панели DNS
2. Nethouse покажет сообщение об успешном сохранении

---

### Шаг 5: Проверка DNS записей

#### 5.1. Подожди распространения DNS

- Обычно занимает **5–30 минут**
- Иногда до **48 часов** (но обычно быстро)

#### 5.2. Проверь на dnschecker.org

1. Открой https://www.dnschecker.org/
2. Введи свой домен (например: `example.com`)
3. Выбери тип записи: **CNAME** или **A**
4. Нажми **Search**
5. Убедись, что записи появились по всему миру

#### 5.3. Проверь в терминале (опционально)

**Windows PowerShell:**
```powershell
nslookup example.com
nslookup api.example.com
```

**Или с указанием DNS сервера:**
```powershell
nslookup example.com 8.8.8.8
```

---

### Шаг 6: Обновление переменных окружения на Render

#### 6.1. Frontend Environment

Зайди в Render → **Static Site** → **Environment**:

```env
VITE_BACKEND_URL=https://api.example.com
VITE_TELEGRAM_BOT_USERNAME=твой_бот
VITE_REQUIRE_AUTH=true
```

**Замени `example.com` на свой домен!**

#### 6.2. Backend Environment

Зайди в Render → **Web Service** → **Environment**:

```env
FRONTEND_URL=https://example.com
ALLOWED_ORIGINS=https://example.com,https://www.example.com
AUTH_JWT_SECRET=c9a13d9a299a6b78c2a51d30ddb865a6b787d02c9747bddfedd33617512313dd
TELEGRAM_BOT_TOKEN=<твой_токен>
GIGACHAT_AUTH_KEY=<твой_ключ>
DATABASE_URL=<твой_DATABASE_URL>
# ... остальные переменные
```

**Замени `example.com` на свой домен!**

**Важно:**
- В `ALLOWED_ORIGINS` укажи **оба варианта** (с `www` и без)
- Используй **https://** (не http://)
- Не добавляй слэш в конце

---

### Шаг 7: Ожидание SSL сертификата

После настройки DNS:

1. **Render автоматически выпустит SSL сертификат** (Let's Encrypt)
2. Это может занять **до 1 часа** после настройки DNS
3. Проверь статус в Render → **Custom Domains**:
   - Должен быть статус **"Verified"** или **"Active"**
   - SSL статус: **"Certificate Issued"** или **"Active"**

---

### Шаг 8: Перезапуск сервисов

#### 8.1. Frontend (Static Site)

1. Зайди в Render → **Static Site** → **Manual Deploy**
2. Нажми **Deploy latest commit**
3. Дождись завершения деплоя

#### 8.2. Backend (Web Service)

1. Зайди в Render → **Web Service** → **Manual Deploy**
2. Нажми **Deploy latest commit**
3. Дождись завершения деплоя

---

### Шаг 9: Проверка работы

#### 9.1. Проверь Frontend

1. Открой свой домен: `https://example.com`
2. Должен открыться твой сайт (не страница Render или Nethouse)
3. Открой консоль браузера (F12) → **Network**
4. Убедись, что запросы идут на `https://api.example.com`

#### 9.2. Проверь Backend

1. Открой `https://api.example.com/health`
2. Должен вернуться JSON: `{"status":"ok",...}`

#### 9.3. Проверь авторизацию

1. Попробуй войти через Telegram
2. Убедись, что запросы проходят без CORS ошибок

---

## 🔍 Примеры DNS записей на Nethouse

### Пример 1: Домен `example.com` (без www)

**DNS записи в Nethouse:**

| Тип   | Имя | Значение                            | TTL  |
|-------|-----|-------------------------------------|------|
| CNAME | @   | go-doc-generator.onrender.com       | 3600 |
| CNAME | api | go-doc-generator-backend.onrender.com | 3600 |

**Переменные окружения:**

Frontend:
```
VITE_BACKEND_URL=https://api.example.com
```

Backend:
```
FRONTEND_URL=https://example.com
ALLOWED_ORIGINS=https://example.com
```

---

### Пример 2: Домен `example.com` (с www и без)

**DNS записи в Nethouse:**

| Тип   | Имя | Значение                            | TTL  |
|-------|-----|-------------------------------------|------|
| CNAME | @   | go-doc-generator.onrender.com       | 3600 |
| CNAME | www | go-doc-generator.onrender.com       | 3600 |
| CNAME | api | go-doc-generator-backend.onrender.com | 3600 |

**Переменные окружения:**

Frontend:
```
VITE_BACKEND_URL=https://api.example.com
```

Backend:
```
FRONTEND_URL=https://example.com
ALLOWED_ORIGINS=https://example.com,https://www.example.com
```

---

### Пример 3: Если Nethouse не поддерживает CNAME для @

**DNS записи в Nethouse:**

| Тип | Имя | Значение                            | TTL  |
|-----|-----|-------------------------------------|------|
| A   | @   | 35.157.123.45 (IP от Render)        | 3600 |
| CNAME | www | go-doc-generator.onrender.com       | 3600 |
| CNAME | api | go-doc-generator-backend.onrender.com | 3600 |

**⚠️ Внимание:** Render покажет нужный IP адрес при добавлении домена в **Custom Domains**.

---

## ⚠️ Решение проблем

### Проблема: CNAME для @ не поддерживается

**Решение:**
1. Render покажет **A запись** вместо CNAME
2. В Nethouse создай A запись для `@` с IP от Render
3. CNAME для `www` оставь как есть

### Проблема: Домен не открывается через 30 минут

**Проверь:**
1. DNS записи на dnschecker.org
2. Правильность записей (нет опечаток)
3. Статус в Render → Custom Domains
4. Попробуй открыть в режиме инкогнито (чтобы обойти кэш)

**Решение:**
1. Подожди еще 30 минут
2. Перезапусти браузер
3. Очисти DNS кэш:
   ```powershell
   ipconfig /flushdns
   ```

### Проблема: SSL сертификат не выдается

**Решение:**
1. Подожди до 1 часа
2. Проверь, что DNS записи распространились (dnschecker.org)
3. В Render → Custom Domains → нажми **Verify** или **Re-verify**
4. Если не работает, удали домен и добавь заново

### Проблема: CORS ошибки после переноса

**Решение:**
1. Проверь `ALLOWED_ORIGINS` в backend Environment
2. Убедись, что указан **https://** (не http://)
3. Убедись, что нет слэша в конце
4. Перезапусти backend (Manual Deploy)

### Проблема: Старый сайт Nethouse все еще открывается

**Решение:**
1. Это нормально, пока DNS не обновился полностью
2. Подожди еще 30 минут
3. Проверь DNS на dnschecker.org
4. Открой в режиме инкогнито

---

## ✅ Чек-лист

- [ ] Получен CNAME target от Render (Frontend)
- [ ] Получен CNAME target от Render (Backend)
- [ ] Зайден в панель Nethouse
- [ ] Найдены DNS настройки домена
- [ ] Удалены старые A записи на Nethouse (если были)
- [ ] Добавлена CNAME для `@` (или A запись, если CNAME не поддерживается)
- [ ] Добавлена CNAME для `www` (если нужна)
- [ ] Добавлена CNAME для `api` (или другого поддомена backend)
- [ ] Изменения сохранены в Nethouse
- [ ] Проверены DNS записи на dnschecker.org
- [ ] Подождано 30 минут для распространения DNS
- [ ] `VITE_BACKEND_URL` обновлен на Render (Frontend)
- [ ] `FRONTEND_URL` обновлен на Render (Backend)
- [ ] `ALLOWED_ORIGINS` обновлен на Render (Backend)
- [ ] Frontend пересобран (Manual Deploy)
- [ ] Backend перезапущен (Manual Deploy)
- [ ] Домен добавлен в Render (Custom Domains)
- [ ] SSL сертификат выдан (проверено в Render)
- [ ] Домен открывается в браузере
- [ ] Backend доступен по поддомену
- [ ] Авторизация работает
- [ ] CORS ошибок нет

---

## 📞 Поддержка

Если возникли проблемы:

1. **Nethouse поддержка:**
   - https://www.nethouse.ru/support
   - Телефон: указан на сайте

2. **Render поддержка:**
   - https://render.com/docs
   - Чат в Dashboard

---

## 🎉 Готово!

После выполнения всех шагов твой домен будет работать на Render!

**Проверь:**
- Frontend: `https://example.com`
- Backend: `https://api.example.com/health`



# 🔧 Решение проблемы с ADMIN_SECRET

## ❌ Проблема

При попытке создать тестового пользователя получаете:
```
401 Unauthorized - Неавторизован. Требуется правильный секретный ключ.
```

Это значит, что `ADMIN_SECRET` на Render **не совпадает** с тем, что вы используете в команде.

## ✅ Решение

### Вариант 1: Узнать правильный ADMIN_SECRET (рекомендуется)

1. Зайдите на https://dashboard.render.com
2. Откройте ваш **Backend сервис** (`api.prepromo.online`)
3. Перейдите в **Environment** → **Environment Variables**
4. Найдите переменную `ADMIN_SECRET`
5. **Скопируйте её значение** (может быть скрыто, нажмите "Reveal" или "Show")

После этого используйте этот секрет:

```bash
node test-create-user.js https://api.prepromo.online "СКОПИРОВАННЫЙ-СЕКРЕТ"
```

### Вариант 2: Установить новый известный ADMIN_SECRET

Если вы не помните, какой секрет установлен, можно установить новый:

1. Зайдите на https://dashboard.render.com
2. Откройте **Backend сервис**
3. **Environment** → **Environment Variables**
4. Найдите `ADMIN_SECRET` и **измените** на: `admin-secret-2025-yookassa`
5. **Сохраните** и подождите, пока сервис перезапустится (1-2 минуты)

После этого создайте пользователя:

```bash
node test-create-user.js https://api.prepromo.online admin-secret-2025-yookassa
```

### Вариант 3: Создать через онлайн сервис

Если знаете ADMIN_SECRET:

1. Откройте https://reqbin.com/curl
2. Method: **POST**
3. URL: `https://api.prepromo.online/api/admin/create-test-user`
4. Headers: `Content-Type: application/json`
5. Body:
   ```json
   {
     "secret": "ВАШ-ADMIN-SECRET-ИЗ-RENDER"
   }
   ```
6. Нажмите "Send"

## 📋 После успешного создания

После успешного создания пользователя вы сможете войти:

- **URL:** `https://prepromo.online/#/auth/test-login`
- **Логин:** `yookassa_test`
- **Пароль:** `YooKassa2025!Test`

## 💡 Важно

- `ADMIN_SECRET` должен **точно совпадать** с тем, что на Render
- Регистр важен (заглавные/строчные буквы)
- Пробелы в начале/конце тоже учитываются


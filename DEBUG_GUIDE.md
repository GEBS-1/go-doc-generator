# 🔍 Руководство по отладке GigaChat API

## ❌ Проблема: "GigaChat API ключ не настроен. Используется демо-режим"

### 🔧 Шаг 1: Проверьте наличие `.env` файла

Файл `.env` должен находиться в **корне проекта** (там же, где `package.json`).

```bash
# Проверка (Windows PowerShell)
Test-Path .env

# Проверка (Linux/Mac)
test -f .env && echo "Файл существует" || echo "Файл НЕ найден"
```

### 🔧 Шаг 2: Проверьте формат `.env` файла

**❌ НЕПРАВИЛЬНО (с пробелами):**
```env
VITE_GIGACHAT_AUTH_KEY = your_key_here
VITE_GIGACHAT_CLIENT_ID = your_id_here
```

**✅ ПРАВИЛЬНО (без пробелов):**
```env
VITE_GIGACHAT_AUTH_KEY=your_key_here
VITE_GIGACHAT_CLIENT_ID=your_id_here
```

**Важно:** 
- НЕ должно быть пробелов вокруг знака `=`
- Значение должно быть на одной строке (не разбивать на несколько строк)
- Не должно быть кавычек вокруг значения (если они не являются частью самого ключа)

### 🔧 Шаг 3: Проверьте содержимое `.env`

Откройте консоль браузера (F12) и проверьте логи:

```
🔍 GigaChat API Configuration Diagnostic
  Environment: development
  VITE_GIGACHAT_AUTH_KEY: ✓ Найден (ZTZiMDFmNTIt...)
  VITE_GIGACHAT_CLIENT_ID: ✗ Не найден
  VITE_GIGACHAT_CLIENT_SECRET: ✗ Не найден
✅ Ключи найдены, API должен работать
```

Если видите `✗ Не найден`, значит:
1. Файл `.env` не существует или неправильно расположен
2. Формат переменных неправильный (пробелы, переносы строк)
3. Dev сервер не был перезапущен после изменения `.env`

### 🔧 Шаг 4: Перезапустите dev сервер

**ВАЖНО:** После изменения `.env` файла **ОБЯЗАТЕЛЬНО** перезапустите dev сервер:

```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

Vite загружает переменные окружения только при запуске!

### 🔧 Шаг 5: Проверьте логи в консоли браузера

Откройте консоль браузера (F12 → Console) и найдите:

1. **При загрузке страницы:**
   ```
   🔍 GigaChat API Configuration Diagnostic
   ```

2. **При генерации структуры:**
   ```
   🔍 StructureEditor: API Keys Check
   ```

3. **При генерации текста:**
   ```
   🔍 TextGeneration: Checking API Keys
   ```

4. **При запросе токена:**
   ```
   🔐 getAccessToken: Checking credentials
   ```

### 🔧 Шаг 6: Типичные ошибки

#### Ошибка 1: Пробелы в `.env`
```
VITE_GIGACHAT_AUTH_KEY = key  ❌
VITE_GIGACHAT_AUTH_KEY=key    ✅
```

#### Ошибка 2: Перенос строки в значении
```
VITE_GIGACHAT_AUTH_KEY=ZTZiMDFmNTItNGYyYS00NzAyLWE1ZDEtYjBjNmQ1YzNiMzg2OmNlYjU5NGVlLTE2OTktNGQyYy04YzE1LTBlNTBmOWI0MTU4Zg==  ❌ (разбито на две строки)

VITE_GIGACHAT_AUTH_KEY=ZTZiMDFmNTItNGYyYS00NzAyLWE1ZDEtYjBjNmQ1YzNiMzg2OmNlYjU5NGVlLTE2OTktNGQyYy04YzE1LTBlNTBmOWI0MTU4Zg==  ✅ (одна строка)
```

#### Ошибка 3: Кавычки вокруг значения
```
VITE_GIGACHAT_AUTH_KEY="your_key"  ❌
VITE_GIGACHAT_AUTH_KEY=your_key    ✅
```

#### Ошибка 4: Не перезапущен сервер
После изменения `.env` нужно перезапустить `npm run dev`

### 🔧 Шаг 7: Автоматическая проверка

Скопируйте и выполните в PowerShell (в корне проекта):

```powershell
# Проверка наличия .env
if (Test-Path .env) {
    Write-Host "✓ .env файл найден" -ForegroundColor Green
    
    # Проверка формата
    $content = Get-Content .env -Raw
    if ($content -match 'VITE_GIGACHAT_AUTH_KEY\s*=') {
        Write-Host "✓ VITE_GIGACHAT_AUTH_KEY найден" -ForegroundColor Green
    } else {
        Write-Host "✗ VITE_GIGACHAT_AUTH_KEY НЕ найден" -ForegroundColor Red
    }
    
    # Проверка пробелов
    if ($content -match 'VITE_GIGACHAT.*\s+=') {
        Write-Host "⚠ ВНИМАНИЕ: Найдены пробелы вокруг знака =" -ForegroundColor Yellow
        Write-Host "  Исправьте формат в .env файле!" -ForegroundColor Yellow
    }
} else {
    Write-Host "✗ .env файл НЕ найден!" -ForegroundColor Red
    Write-Host "  Скопируйте env.example в .env" -ForegroundColor Yellow
}
```

## 📋 Правильный формат `.env` файла

```env
# GigaChat API Configuration
# ВАРИАНТ 1 (рекомендуется): Authorization Key
VITE_GIGACHAT_AUTH_KEY=ZTZiMDFmNTItNGYyYS00NzAyLWE1ZDEtYjBjNmQ1YzNiMzg2OmNlYjU5NGVlLTE2OTktNGQyYy04YzE1LTBlNTBmOWI0MTU4Zg==

# ВАРИАНТ 2 (альтернатива): Client ID + Client Secret
# VITE_GIGACHAT_CLIENT_ID=e6b01f52-4f2a-4702-a5d1-b0c6d5c3b386
# VITE_GIGACHAT_CLIENT_SECRET=ceb594ee-1699-4d2c-8c15-0e50f9b4158f
```

## ✅ Что делать, если все еще не работает

1. **Откройте консоль браузера (F12)**
2. **Найдите группу логов** `🔍 GigaChat API Configuration Diagnostic`
3. **Скопируйте все сообщения из консоли**
4. **Проверьте терминал**, где запущен `npm run dev` - там тоже могут быть ошибки

## 🚀 После исправления

1. Исправьте `.env` файл (уберите пробелы, исправьте переносы строк)
2. Сохраните файл
3. **ОБЯЗАТЕЛЬНО перезапустите dev сервер:**
   ```bash
   # Ctrl+C для остановки
   npm run dev
   ```
4. Обновите страницу в браузере (F5)
5. Проверьте логи в консоли

---

**Последнее обновление:** 2025-01-27  
**Статус:** ✅ Готово к использованию

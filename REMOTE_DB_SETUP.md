# 🌐 Настройка подключения к удаленной базе данных

## 📋 Данные, которые мне нужны от вас

Для подключения к вашей удаленной БД на сервере (Render, Supabase, Railway и т.д.) нужны следующие данные:

### 1. **Хост (Host)**
   - Например: `dpg-xxxxx-a.oregon-postgres.render.com`
   - Или: `your-db.example.com`
   - Или IP-адрес: `123.456.789.0`

### 2. **Порт (Port)**
   - Обычно: `5432` (стандартный порт PostgreSQL)
   - Или другой порт, если указан в настройках БД

### 3. **Имя базы данных (Database Name)**
   - Например: `docugen`
   - Или: `docugen_xxxxx`

### 4. **Пользователь (Username)**
   - Например: `docugen_user`
   - Или: `telebot`

### 5. **Пароль (Password)**
   - Пароль для доступа к БД

### 6. **SSL (обязательно для удаленных БД)**
   - Обычно требуется для Render, Supabase, Railway, Neon и т.д.
   - Нужно добавить `?sslmode=require` в строку подключения

---

## 🔗 Формат DATABASE_URL

После того как вы предоставите данные, я настрою строку подключения в формате:

```
postgresql://[пользователь]:[пароль]@[хост]:[порт]/[база_данных]?sslmode=require
```

**Пример для Render:**
```
postgresql://docugen_user:password123@dpg-abc123-a.oregon-postgres.render.com:5432/docugen_abc123?sslmode=require
```

**Пример для Supabase:**
```
postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres?sslmode=require
```

---

## 📝 Где найти эти данные?

### **Если база на Render:**
1. Зайдите в Dashboard Render
2. Выберите ваш PostgreSQL сервис
3. В разделе "Info" найдите:
   - **Internal Database URL** или **External Database URL**
   - Скопируйте всю строку - там уже всё готово!

### **Если база на Supabase:**
1. Зайдите в Supabase Dashboard
2. Выберите ваш проект
3. Settings → Database
4. В разделе "Connection string" выберите "URI"
5. Скопируйте строку подключения

### **Если база на Railway/Neon/DigitalOcean:**
1. Зайдите в панель управления сервиса
2. Найдите раздел "Connection" или "Database"
3. Там будет готовая строка подключения или отдельные параметры

---

## ✅ Что я сделаю после получения данных

1. Обновлю `DATABASE_URL` в `backend/.env`
2. Добавлю автоматическую настройку SSL для удаленных подключений
3. Проверю подключение к БД
4. Создам тестового пользователя для Юкассы

---

## 🔒 Безопасность

**ВАЖНО:** 
- Не публикуйте данные БД в открытом доступе
- Используйте переменные окружения
- На сервере Render/Supabase используйте встроенные переменные окружения

Вы можете отправить мне данные любым удобным способом, я обновлю конфигурацию!


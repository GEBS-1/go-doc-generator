# 🚀 Создание тестового пользователя - Инструкция

## ✅ Что уже сделано

Вы добавили `ADMIN_SECRET` на Render - отлично! Теперь нужно создать тестового пользователя.

## 📋 Что нужно для создания

Мне нужен **URL вашего backend сервиса на Render**, чтобы вызвать API endpoint.

### Как найти URL backend:

1. Зайдите в **Render Dashboard**: https://dashboard.render.com
2. Найдите ваш **Backend сервис** (обычно называется что-то вроде "go-doc-generator-backend" или похожее)
3. Откройте его
4. В разделе "Info" или вверху страницы вы увидите URL, например:
   - `https://go-doc-generator-backend.onrender.com`
   - Или `https://your-backend-name.onrender.com`

## 🚀 Варианты создания пользователя

### Вариант 1: Автоматически (если укажете URL)

Если вы предоставите URL backend, я могу:
1. Создать скрипт с вашим URL
2. Вызвать API endpoint автоматически
3. Создать тестового пользователя

### Вариант 2: Вручную через curl

Скопируйте команду ниже, заменив `YOUR_BACKEND_URL` на ваш реальный URL:

```bash
curl -X POST https://YOUR_BACKEND_URL.onrender.com/api/admin/create-test-user \
  -H "Content-Type: application/json" \
  -d "{\"secret\": \"admin-secret-2025-yookassa\"}"
```

**Важно:** Замените `admin-secret-2025-yookassa` на тот секрет, который вы добавили в Render!

### Вариант 3: Через браузер (Postman/Insomnia)

1. Откройте Postman или Insomnia
2. Создайте новый POST запрос
3. URL: `https://YOUR_BACKEND_URL.onrender.com/api/admin/create-test-user`
4. Headers: `Content-Type: application/json`
5. Body (JSON):
   ```json
   {
     "secret": "ваш-admin-secret"
   }
   ```
6. Отправьте запрос

## ✅ Что произойдет после создания

После успешного создания вы получите:

```json
{
  "success": true,
  "credentials": {
    "login": "yookassa_test",
    "password": "YooKassa2025!Test"
  },
  "message": "Тестовый пользователь успешно создан"
}
```

## 📝 Что дальше

После создания пользователя:
1. Обновите URL в `YOOKASSA_CREDENTIALS.txt` (укажите URL вашего сайта)
2. Отправьте файл команде Юкассы

---

**Скажите мне URL вашего backend сервиса, и я создам пользователя автоматически!** 🚀

Или выполните команду curl вручную - она тоже сработает.


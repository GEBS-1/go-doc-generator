# 💳 Система оплаты токенов

## ✅ Реализовано

### Backend

1. **Таблицы в БД** (`backend/db.js`):
   - `token_usage` - учет использования токенов
   - `free_downloads` - отслеживание бесплатных скачиваний

2. **Endpoints** (`backend/server.js`):
   - `POST /api/tokens/record` - сохранение токенов после генерации
   - `GET /api/tokens/unpaid` - получение неоплаченных токенов
   - `POST /api/payments/create-tokens` - создание платежа за токены

3. **Логика квот** (`evaluateDocumentQuota`):
   - Проверка неоплаченных токенов
   - Блокировка скачивания при наличии неоплаченных токенов
   - Поддержка бесплатного скачивания (1 документ)
   - Учет documentId для отслеживания

4. **Webhook** (`/api/payments/webhook`):
   - Обработка оплаты токенов
   - Отметка токенов как оплаченных после успешной оплаты

### Frontend

1. **Компонент TokenPaymentModal** (`src/components/TokenPaymentModal.tsx`):
   - Отображение неоплаченных токенов
   - Расчет стоимости
   - Создание платежа через YooKassa
   - Открытие страницы оплаты в новом окне

2. **Интеграция в TitlePage** (`src/components/steps/TitlePage.tsx`):
   - Обработка ошибки `unpaid_tokens`
   - Автоматическое открытие модального окна при ошибке
   - Проверка квоты с documentId
   - Передача documentId при скачивании

## 🔧 Как это работает

### Сценарий 1: Первое бесплатное скачивание
1. Пользователь генерирует документ
2. При скачивании создается `documentId`
3. Проверяется, использовано ли бесплатное скачивание
4. Если нет - документ скачивается бесплатно
5. Запись в `free_downloads` с `documentId`

### Сценарий 2: Второе и последующие скачивания
1. Пользователь генерирует новый документ
2. Создается `documentId`
3. Проверяется квота:
   - Бесплатное скачивание уже использовано
   - Генерируются токены
4. Токены сохраняются в `token_usage` как неоплаченные
5. При скачивании проверяются неоплаченные токены
6. Если есть - показывается модальное окно с предложением оплаты

### Сценарий 3: Оплата токенов
1. Пользователь видит модальное окно с неоплаченными токенами
2. Нажимает "Оплатить"
3. Создается платеж через YooKassa
4. Открывается страница оплаты
5. После успешной оплаты:
   - Webhook обрабатывает событие
   - Все неоплаченные токены помечаются как оплаченные
   - Пользователь может скачать документ

## 💰 Расчет стоимости

- Цена за токен: **0.01 ₽**
- Расчет: `cost_rub = total_tokens * 0.01`
- Сохраняется в `token_usage.cost_rub`

## 🎯 Ключевые особенности

1. **Блокировка скачивания** - документ нельзя скачать, пока не оплачены токены
2. **Автоматическое открытие модального окна** - при попытке скачать с неоплаченными токенами
3. **Отслеживание по documentId** - каждый документ имеет уникальный ID
4. **Бесплатное первое скачивание** - для плана "free"

## 📝 API Endpoints

### GET /api/tokens/unpaid
Возвращает список неоплаченных токенов пользователя.

**Ответ:**
```json
{
  "unpaidTokens": [
    {
      "id": 1,
      "documentId": "uuid",
      "promptTokens": 100,
      "completionTokens": 200,
      "totalTokens": 300,
      "costRub": 3.00,
      "createdAt": "2025-01-27T10:00:00Z"
    }
  ],
  "summary": {
    "totalCost": 3.00,
    "totalTokens": 300,
    "count": 1
  }
}
```

### POST /api/tokens/record
Сохраняет использование токенов после генерации.

**Запрос:**
```json
{
  "documentId": "uuid",
  "promptTokens": 100,
  "completionTokens": 200,
  "totalTokens": 300
}
```

### POST /api/payments/create-tokens
Создает платеж за неоплаченные токены.

**Ответ:**
```json
{
  "paymentId": "payment_id",
  "confirmationUrl": "https://yookassa.ru/...",
  "amount": 3.00,
  "tokenCount": 1
}
```

## 🔄 Следующие шаги

1. Интегрировать сохранение токенов после генерации документа
2. Добавить отображение истории токенов в профиле
3. Добавить статистику по токенам



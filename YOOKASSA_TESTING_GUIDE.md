# 🧪 Руководство по тестированию YooKassa

## 📋 Содержание
1. [Настройка тестового магазина](#настройка-тестового-магазина)
2. [Получение тестовых ключей](#получение-тестовых-ключей)
3. [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Тестирование платежей](#тестирование-платежей)
5. [Настройка webhook](#настройка-webhook)
6. [Тестовые карты](#тестовые-карты)
7. [Отладка проблем](#отладка-проблем)

---

## 🏪 Настройка тестового магазина

### Шаг 1: Создание тестового магазина в YooKassa

1. Зайдите на [https://yookassa.ru/](https://yookassa.ru/)
2. Войдите в личный кабинет (или зарегистрируйтесь)
3. Перейдите в раздел **"Настройки"** → **"Магазины"**
4. Создайте новый магазин или используйте существующий
5. **ВАЖНО**: Убедитесь, что магазин находится в **тестовом режиме**

---

## 🔑 Получение тестовых ключей

### Где найти тестовые ключи:

1. В личном кабинете YooKassa перейдите в **"Настройки"** → **"API-ключи"**
2. Найдите раздел **"Тестовые ключи"** (для продакшн будут "Боевые ключи")
3. Скопируйте:
   - **shopId** (ID магазина) - например: `123456`
   - **secretKey** (Секретный ключ) - начинается с `test_` или `live_`

**Пример тестового ключа**: `test_MjIyNjMyNzQzNC0xLVBTVS1OdmVjT...`

---

## ⚙️ Настройка переменных окружения

### Файл: `backend/.env`

```env
# YooKassa тестовые ключи
YOOKASSA_SHOP_ID=123456
YOOKASSA_SECRET_KEY=test_MjIyNjMyNzQzNC0xLVBTVS1OdmVjT...

# URL для возврата после оплаты (для локального тестирования)
FRONTEND_URL=http://localhost:8080
PAYMENT_RETURN_URL=http://localhost:8080/payment/success
PAYMENT_FAIL_URL=http://localhost:8080/payment/failed

# Остальные переменные...
AUTH_JWT_SECRET=your-secret-key
TELEGRAM_BOT_TOKEN=your-bot-token
```

### Проверка конфигурации

После добавления переменных перезапустите backend сервер:

```bash
cd backend
npm run dev
```

В консоли должна появиться запись о том, что YooKassa инициализирован (если нет предупреждения `[Payments] YooKassa переменные окружения не заданы`).

---

## 🧪 Тестирование платежей

### 1. Проверка создания платежа

**Эндпоинт**: `POST /api/payments/create`
**Требуется**: Авторизация (JWT токен)

**Запрос:**
```bash
curl -X POST http://localhost:3001/api/payments/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{"planId": "basic"}'
```

**Ожидаемый ответ (успех):**
```json
{
  "status": "pending",
  "paymentId": "2c8cb95e-0001-5000-8000-1a3d2e4f5g6h",
  "confirmationUrl": "https://yoomoney.ru/checkout/payments/v2/contract?orderId=..."
}
```

**Ожидаемый ответ (бесплатный план):**
```json
{
  "status": "activated",
  "user": { ... }
}
```

### 2. Тестирование через Frontend

1. Откройте приложение: `http://localhost:8080`
2. Войдите через Telegram
3. Перейдите на страницу выбора тарифа
4. Выберите платный план (например, "Базовый" - 199 ₽)
5. Нажмите "Оплатить"
6. Должно произойти перенаправление на YooKassa

---

## 🔗 Настройка Webhook

Webhook нужен для автоматической активации подписки после успешной оплаты.

### Для локального тестирования (с ngrok)

1. **Установите ngrok**:
   ```bash
   # Windows (через Chocolatey)
   choco install ngrok
   
   # Или скачайте с https://ngrok.com/
   ```

2. **Запустите ngrok**:
   ```bash
   ngrok http 3001
   ```

3. **Скопируйте HTTPS URL** (например: `https://abc123.ngrok.io`)

4. **Настройте webhook в YooKassa**:
   - Личный кабинет → Настройки → HTTP-уведомления
   - URL: `https://abc123.ngrok.io/api/payments/webhook`
   - Тип события: `payment.succeeded`, `payment.canceled`

### Для продакшн (Render/VPS)

1. Получите публичный URL вашего backend (например: `https://your-backend.onrender.com`)
2. Настройте webhook в YooKassa:
   - URL: `https://your-backend.onrender.com/api/payments/webhook`
   - Тип события: `payment.succeeded`, `payment.canceled`

### Проверка webhook

После настройки webhook, YooKassa отправит тестовое уведомление. Проверьте логи backend:

```bash
[Payments] Webhook received: payment.succeeded
[Payments] Payment successful, activating subscription...
```

---

## 💳 Тестовые карты

YooKassa предоставляет тестовые карты для симуляции различных сценариев.

### Успешная оплата:
- **Номер карты**: `5555555555554444`
- **Срок действия**: любая будущая дата (например: `12/25`)
- **CVC**: любые 3 цифры (например: `123`)
- **Имя**: любые данные

### Отклоненная оплата:
- **Номер карты**: `5555555555554477`
- Остальное аналогично

### Полный список тестовых карт:
См. официальную документацию YooKassa: [Тестовые данные](https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing#test-cards)

---

## 🔍 Процесс тестирования (пошагово)

### Сценарий 1: Успешная оплата

1. **Создайте платеж** через frontend или API
2. **Перейдите по ссылке** `confirmationUrl`
3. **Введите тестовые данные карты**: `5555555555554444`
4. **Подтвердите оплату**
5. **Проверьте**:
   - ✅ Перенаправление на `/payment/success`
   - ✅ Получение webhook в backend (в логах)
   - ✅ Активация подписки в БД
   - ✅ Уведомление в Telegram (если настроен бот)

### Сценарий 2: Отмена оплаты

1. Создайте платеж
2. На странице оплаты нажмите "Отмена"
3. **Проверьте**:
   - ✅ Перенаправление на `/payment/failed`
   - ✅ Платеж не активирован

### Сценарий 3: Отклоненная карта

1. Создайте платеж
2. Используйте карту `5555555555554477`
3. **Проверьте**:
   - ✅ Ошибка оплаты
   - ✅ Платеж не активирован

---

## 🐛 Отладка проблем

### Проблема: "Платёжный шлюз не настроен"

**Причина**: Не заданы переменные окружения

**Решение**:
1. Проверьте файл `backend/.env`
2. Убедитесь, что переменные `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY` установлены
3. Перезапустите backend сервер

---

### Проблема: "Не удалось создать платёж"

**Причина**: Неверные ключи или недоступен API YooKassa

**Решение**:
1. Проверьте логи backend:
   ```bash
   YooKassa create payment error: ...
   ```
2. Убедитесь, что используете **тестовые ключи** (начинаются с `test_`)
3. Проверьте, что магазин в тестовом режиме

---

### Проблема: Webhook не приходит

**Причина**: Неправильный URL или проблемы с сетью

**Решение**:
1. Для локального тестирования используйте **ngrok**
2. Проверьте, что URL webhook доступен извне
3. Проверьте логи ngrok:
   ```bash
   POST /api/payments/webhook 200 OK
   ```
4. В личном кабинете YooKassa проверьте статус webhook (должен быть активен)

---

### Проблема: Подписка не активируется после оплаты

**Причина**: Ошибка в обработке webhook или БД

**Решение**:
1. Проверьте логи backend на наличие ошибок
2. Проверьте, что webhook получает событие `payment.succeeded`
3. Проверьте таблицу `payments` в БД - должен быть запись о платеже
4. Проверьте таблицу `subscriptions` - должна быть активирована подписка

---

## 📊 Проверка в базе данных

### Таблица `payments`:
```sql
SELECT * FROM payments 
ORDER BY created_at DESC 
LIMIT 10;
```

**Что проверить**:
- `status` должен быть `'succeeded'` для успешных платежей
- `metadata` должен содержать `userId` и `planId`
- `payment_id` должен соответствовать ID из YooKassa

### Таблица `subscriptions`:
```sql
SELECT * FROM subscriptions 
ORDER BY activated_at DESC 
LIMIT 10;
```

**Что проверить**:
- `plan` должен соответствовать купленному плану
- `status` должен быть `'active'`
- `docs_limit` должен быть установлен правильно
- `activated_at` должна быть установлена

---

## 🔄 Переход на продакшн

Когда тестирование завершено:

1. **Создайте боевой магазин** в YooKassa (или переключите тестовый в боевой режим)
2. **Получите боевые ключи** (начинаются с `live_`)
3. **Обновите переменные окружения**:
   ```env
   YOOKASSA_SHOP_ID=your_production_shop_id
   YOOKASSA_SECRET_KEY=live_xxxxxxxxxxxxx
   ```
4. **Обновите URL webhook** на продакшн URL
5. **Перезапустите backend**

**ВАЖНО**: Боевые ключи используют реальные деньги! Всегда тестируйте сначала в тестовом режиме.

---

## 📝 Чеклист перед запуском в продакшн

- [ ] Протестированы все сценарии оплаты (успех, отмена, ошибка)
- [ ] Webhook настроен и работает
- [ ] Уведомления в Telegram приходят
- [ ] Подписки активируются корректно
- [ ] Лимиты документов работают правильно
- [ ] База данных обновляется корректно
- [ ] Логи ошибок проверены
- [ ] Боевые ключи получены и сохранены безопасно
- [ ] Webhook URL обновлен на продакшн

---

## 🔗 Полезные ссылки

- [Документация YooKassa](https://yookassa.ru/developers/)
- [API Reference](https://yookassa.ru/developers/api)
- [Тестовые данные](https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing)
- [ngrok для локального тестирования](https://ngrok.com/)

---

## 💡 Дополнительные советы

1. **Логирование**: Добавьте детальное логирование всех платежных операций
2. **Мониторинг**: Настройте алерты на ошибки платежей
3. **Бэкап**: Регулярно делайте бэкап таблицы `payments`
4. **Тестирование**: Регулярно тестируйте платежи даже после запуска в продакшн


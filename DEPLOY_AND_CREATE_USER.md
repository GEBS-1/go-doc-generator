# 🚀 Решение: Задеплоить и создать пользователя

## Проблема

Код изменен локально, но НЕ задеплоен на Render. На сервере работает старая версия с проверкой секрета.

## ✅ Решение

### 1. Задеплоить код на Render

**Если автоматический деплой:**
```bash
git add backend/server.js
git commit -m "Temporarily disable admin secret check for test user creation"
git push
```

**Если ручной деплой:**
- Render Dashboard → Backend → Manual Deploy → Deploy latest commit

### 2. Подождать 1-2 минуты (деплой)

### 3. Создать пользователя

```bash
cd backend
node test-create-user.js https://api.prepromo.online any-secret
```

После деплоя проверка секрета будет отключена, и пользователь создастся.

### 4. После создания пользователя

Вернуть проверку секрета в коде и задеплоить снова.


# Исправление проблемы с кнопкой "Войти" на хостинге

## 🔴 ПРОБЛЕМА

Кнопка "Войти" не отображается на хостинге, хотя должна.

## 🔍 ПРИЧИНЫ

1. **Переменные окружения не установлены на Render**
   - `VITE_REQUIRE_AUTH` не установлен или установлен неправильно
   - `VITE_BACKEND_URL` не установлен или установлен неправильно

2. **Vite переменные загружаются только во время сборки**
   - Если переменные добавлены ПОСЛЕ сборки - они не будут доступны
   - Нужно пересобрать проект после добавления переменных

## ✅ РЕШЕНИЕ

### Шаг 1: Проверьте переменные окружения на Render

Зайдите в Render → Frontend Static Site → **Environment**

Должны быть установлены:
```
VITE_REQUIRE_AUTH=true
VITE_BACKEND_URL=https://go-doc-generator-backend.onrender.com
VITE_TELEGRAM_BOT_USERNAME=<ваш_бот_username>
```

### Шаг 2: Пересоберите проект

**ВАЖНО:** После добавления/изменения переменных окружения:

1. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
2. Дождитесь завершения сборки
3. Проверьте логи сборки - не должно быть ошибок

### Шаг 3: Проверьте в браузере

1. Откройте сайт на хостинге
2. Нажмите F12 → Console
3. Найдите логи `[Header] Environment check:`
4. Проверьте значения:
   - `VITE_REQUIRE_AUTH` должно быть `"true"` или `undefined`
   - `authRequired` должно быть `true`
   - `VITE_BACKEND_URL` должно быть URL вашего backend

### Шаг 4: Если кнопка все еще не появляется

1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Откройте сайт в режиме инкогнито
3. Проверьте консоль на ошибки

## 🐛 ОТЛАДКА

### Проверка переменных в консоли браузера:

```javascript
// В консоли браузера выполните:
console.log('VITE_REQUIRE_AUTH:', import.meta.env.VITE_REQUIRE_AUTH);
console.log('VITE_BACKEND_URL:', import.meta.env.VITE_BACKEND_URL);
console.log('VITE_TELEGRAM_BOT_USERNAME:', import.meta.env.VITE_TELEGRAM_BOT_USERNAME);
```

### Ожидаемый результат:

```
VITE_REQUIRE_AUTH: "true"
VITE_BACKEND_URL: "https://go-doc-generator-backend.onrender.com"
VITE_TELEGRAM_BOT_USERNAME: "ваш_бот_username"
```

## 📝 ЧЕКЛИСТ

- [ ] Переменные окружения добавлены на Render (Frontend)
- [ ] Выполнен Manual Deploy после добавления переменных
- [ ] Сборка прошла успешно (проверьте логи)
- [ ] В консоли браузера видны правильные значения переменных
- [ ] Кнопка "Войти" отображается в Header
- [ ] При нажатии на "Войти" открывается диалог авторизации

## ⚠️ ВАЖНО

**Vite переменные окружения (`VITE_*`) встраиваются в код во время сборки!**

Это значит:
- ❌ Нельзя изменить переменные без пересборки
- ✅ Нужно делать Manual Deploy после изменения переменных
- ✅ Переменные должны быть установлены ДО сборки

## 🔧 АЛЬТЕРНАТИВНОЕ РЕШЕНИЕ

Если переменные не работают, можно временно хардкодить значения в коде (НЕ рекомендуется для production):

```tsx
// В src/components/Header.tsx
const authRequired = true; // Временно хардкод
```

Но лучше исправить переменные окружения на Render!


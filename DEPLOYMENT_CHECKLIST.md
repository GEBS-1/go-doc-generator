# ✅ Чеклист для деплоя

## 🎯 Статус: ГОТОВО К ДЕПЛОЮ

**Дата проверки:** 2025-01-27

---

## ✅ Проверенные компоненты

### 1. Backend
- ✅ YooKassa интеграция работает
- ✅ Платежи создаются успешно
- ✅ Webhook endpoint настроен
- ✅ Авторизация через Telegram работает
- ✅ База данных настроена
- ✅ CORS настроен

### 2. Frontend
- ✅ Сборка проходит без ошибок
- ✅ Интеграция с YooKassa работает
- ✅ Авторизация работает
- ✅ UI компоненты работают

### 3. Тестирование
- ✅ Создание платежей протестировано
- ✅ Форма YooKassa открывается корректно
- ✅ Тестовый пользователь создан

---

## ⚠️ ВАЖНО: Перед деплоем

### 1. Тестовый endpoint (ОБЯЗАТЕЛЬНО)
**Файл:** `backend/server.js` (строка 669)

**Действие:** Защитить или удалить тестовый endpoint `/api/test/auth`

**Вариант 1 (рекомендуется):** Защитить проверкой окружения
```javascript
// Добавить проверку только для development
if (process.env.NODE_ENV !== 'production') {
  app.get('/api/test/auth', async (req, res) => {
    // ... существующий код
  });
}
```

**Вариант 2:** Полностью удалить перед деплоем

### 2. Переменные окружения для Production

#### Backend (Render/Heroku/etc):
```env
# Обязательные
DATABASE_URL=postgresql://...
AUTH_JWT_SECRET=ваш_секретный_ключ_минимум_32_символа
TELEGRAM_BOT_TOKEN=ваш_токен_бота

# YooKassa (PRODUCTION ключи!)
YOOKASSA_SHOP_ID=ваш_реальный_shop_id
YOOKASSA_SECRET_KEY=live_ваш_секретный_ключ  # БЕЗ test_ префикса!

# URLs
FRONTEND_URL=https://ваш-домен.com
PAYMENT_RETURN_URL=https://ваш-домен.com/payment/success
PAYMENT_FAIL_URL=https://ваш-домен.com/payment/failed

# CORS
ALLOWED_ORIGINS=https://ваш-домен.com

# GigaChat (опционально, если используете)
GIGACHAT_CLIENT_ID=...
GIGACHAT_CLIENT_SECRET=...
```

#### Frontend (Vercel/Netlify/etc):
```env
# Backend URL
VITE_BACKEND_URL=https://ваш-backend-url.onrender.com

# GigaChat (если используете напрямую)
VITE_GIGACHAT_AUTH_KEY=...
# ИЛИ
VITE_GIGACHAT_CLIENT_ID=...
VITE_GIGACHAT_CLIENT_SECRET=...

# Telegram Bot
VITE_TELEGRAM_BOT_USERNAME=ваш_бот_username
```

### 3. YooKassa - Переход на Production

**ВАЖНО:** 
1. Получите **реальные** ключи от YooKassa (не тестовые!)
2. Убедитесь, что `YOOKASSA_SECRET_KEY` начинается с `live_` (не `test_`)
3. Настройте webhook URL в личном кабинете YooKassa:
   ```
   https://ваш-backend-url.onrender.com/api/payments/webhook
   ```

### 4. База данных
- ✅ PostgreSQL настроена
- ✅ Таблицы созданы автоматически
- ⚠️ Убедитесь, что DATABASE_URL настроен на production БД

### 5. Telegram Bot
- ✅ Бот настроен
- ⚠️ Убедитесь, что `TELEGRAM_BOT_TOKEN` - production токен
- ⚠️ Проверьте, что webhook URL настроен (если используете)

---

## 📋 Пошаговая инструкция деплоя

### Шаг 1: Подготовка кода
```bash
# 1. Защитить тестовый endpoint (см. выше)
# 2. Убедиться, что все изменения закоммичены
git add .
git commit -m "Ready for production deployment"
```

### Шаг 2: Деплой Backend (Render/Heroku)

#### Render:
1. Подключите репозиторий
2. Выберите "Web Service"
3. Настройте:
   - **Build Command:** `cd backend && npm install`
   - **Start Command:** `cd backend && npm start`
   - **Root Directory:** `backend`
4. Добавьте все переменные окружения из списка выше
5. Deploy

#### Heroku:
```bash
heroku create ваш-проект-backend
cd backend
heroku config:set DATABASE_URL=...
heroku config:set AUTH_JWT_SECRET=...
# ... остальные переменные
git subtree push --prefix backend heroku main
```

### Шаг 3: Деплой Frontend (Vercel/Netlify)

#### Vercel:
1. Подключите репозиторий
2. Настройте:
   - **Framework Preset:** Vite
   - **Root Directory:** `.` (корень проекта)
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
3. Добавьте переменные окружения
4. Deploy

#### Netlify:
1. Подключите репозиторий
2. Настройте:
   - **Build command:** `npm run build`
   - **Publish directory:** `dist`
3. Добавьте переменные окружения
4. Deploy

### Шаг 4: Настройка YooKassa
1. Войдите в личный кабинет YooKassa
2. Перейдите в "Настройки" → "Уведомления"
3. Добавьте webhook URL:
   ```
   https://ваш-backend-url.onrender.com/api/payments/webhook
   ```
4. Сохраните **реальные** ключи (не тестовые!)

### Шаг 5: Проверка после деплоя
- [ ] Backend доступен: `https://ваш-backend-url.onrender.com/health`
- [ ] Frontend открывается
- [ ] Авторизация через Telegram работает
- [ ] Создание платежа работает
- [ ] Webhook от YooKassa получается

---

## 🔒 Безопасность

### ✅ Уже настроено:
- JWT токены для авторизации
- CORS для разрешенных origins
- HTTP-only cookies
- Переменные окружения не в Git

### ⚠️ Рекомендуется добавить:
- Rate limiting для API
- HTTPS только (в production)
- Мониторинг ошибок (Sentry)
- Логирование запросов

---

## 📝 Файлы, которые НЕ должны попасть в production

Эти файлы уже в `.gitignore`:
- ✅ `.env` файлы
- ✅ `node_modules`
- ✅ Тестовые скрипты (`create-test-user.js`, `test-payment.js`)
- ✅ `public/test-auth.html` (можно удалить или оставить для локального тестирования)

---

## 🚨 Критичные моменты

1. **YooKassa ключи:** Обязательно используйте РЕАЛЬНЫЕ ключи (не тестовые!)
2. **Тестовый endpoint:** Защитите или удалите `/api/test/auth`
3. **HTTPS:** Все URL должны быть HTTPS в production
4. **CORS:** Убедитесь, что `ALLOWED_ORIGINS` содержит ваш production домен
5. **Webhook:** Настройте webhook URL в YooKassa

---

## ✅ Готово к деплою!

После выполнения всех пунктов выше, можно деплоить. 🚀

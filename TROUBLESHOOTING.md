# 🛠️ Troubleshooting Guide

## 🛑 **Как остановить программу в терминале**

### **Вариант 1: PowerShell (Windows)**
```powershell
# Остановить все Node.js процессы
Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force

# Проверить, что остановлено
netstat -ano | findstr :8080
# Если ничего не выводится - остановлено ✅
```

### **Вариант 2: CMD (Windows)**
```cmd
# Найти процесс на порту 8080
netstat -ano | findstr :8080

# Остановить по PID (замените XXXX на номер процесса)
taskkill /PID XXXX /F
```

### **Вариант 3: Если запущено в PowerShell**
```powershell
# Просто нажмите Ctrl+C в терминале где запущен npm run dev
```

### **Вариант 4: Linux/Mac**
```bash
# Найти процесс на порту 8080
lsof -i :8080

# Остановить по PID
kill -9 PID

# Или остановить все Node.js процессы
pkill node
```

---

## 🔄 **Как перезапустить**

### **После остановки**
```powershell
# Если в новой сессии терминала
npm run dev

# Если в той же сессии
# Просто запустите снова
npm run dev
```

### **После изменения .env**
```powershell
# 1. Остановите сервер (Ctrl+C или команды выше)
# 2. Запустите заново
npm run dev
```

---

## 📋 **Полезные команды**

### **Проверить запущен ли сервер**
```powershell
netstat -ano | findstr :8080
# Если выводится что-то - сервер запущен
```

### **Посмотреть все Node.js процессы**
```powershell
Get-Process | Where-Object {$_.ProcessName -eq "node"}
```

### **Очистить кэш и переустановить зависимости**
```powershell
# Остановите сервер сначала!

# Удалить node_modules и package-lock
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json

# Переустановить
npm install

# Запустить снова
npm run dev
```

---

## 🐛 **Типичные проблемы**

### **Порт 8080 занят**
```powershell
# Проверить кто занимает порт
netstat -ano | findstr :8080

# Остановить (замените XXXX на PID)
taskkill /PID XXXX /F

# Или изменить порт в vite.config.ts
server: {
  port: 3000,  // другой порт
}
```

### **npm run dev не запускается**
```powershell
# Проверить установлены ли зависимости
ls node_modules

# Если нет - установить
npm install

# Проверить версию Node.js
node -v
# Должна быть >= 18

# Проверить npm
npm -v
# Должна быть >= 9
```

### **Изменения в коде не видны**
```powershell
# Остановите и перезапустите
# Ctrl+C
npm run dev
```

### **Build не работает**
```powershell
# Очистить dist папку
Remove-Item -Recurse -Force dist

# Пересобрать
npm run build

# Проверить ошибки
npm run build 2>&1 | Select-String -Pattern "Error"
```

---

## 📝 **Чеклист при проблемах**

- [ ] Сервер остановлен?
- [ ] Зависимости установлены? (`npm install`)
- [ ] .env файл настроен? (`Get-Content .env`)
- [ ] Порт не занят? (`netstat -ano | findstr :8080`)
- [ ] Node.js установлен? (`node -v`)
- [ ] Нет синтаксических ошибок? (`npm run build`)
- [ ] Git репозиторий актуален? (`git status`)

---

## 🔍 **Полезные утилиты**

### **Проверка проекта**
```powershell
# Валидация файлов
node test-flow.cjs

# Проверка .env
node check-env.cjs

# Linting
npm run lint
```

---

## ✅ **Быстрые решения**

| Проблема | Решение |
|----------|---------|
| Сервер не запускается | `npm install` → `npm run dev` |
| Порт занят | Остановить процесс на порту 8080 |
| Изменения не видны | Перезапустить dev сервер |
| Build ошибки | `npm run build` и проверить вывод |
| .env не читается | Проверить кодировку (UTF-8) и перезапустить |
| Node_modules проблемы | Удалить и переустановить |

---

**Дополнительная документация**: 
- `README.md` - основная документация
- `TEST_PLAN.md` - план тестирования
- `FIXES_REPORT.md` - решения проблем

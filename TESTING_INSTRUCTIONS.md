# 🧪 Инструкция по тестированию новой системы биллинга

## ✅ Что было реализовано

1. **Новая система биллинга:**
   - 1 бесплатное скачивание для каждого пользователя
   - Оплата по токенам: 0.01 ₽ за токен
   - Проверка неоплаченных токенов перед скачиванием

2. **Предпросмотр документа:**
   - Кнопка "Предпросмотр" на этапе титульного листа
   - Модальное окно с полным содержимым документа

3. **Трекинг токенов:**
   - Автоматический подсчет токенов при генерации
   - Сохранение в БД после скачивания
   - API для работы с неоплаченными токенами

---

## 🚀 Запуск системы

### 1. Проверка backend/.env

Убедитесь, что `backend/.env` содержит все необходимые переменные:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `AUTH_JWT_SECRET` - секретный ключ для JWT (минимум 32 символа)
- `GIGACHAT_AUTH_KEY` - ключ GigaChat API
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота (если используется)
- `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY` - для оплаты (если используется)

### 2. Запуск backend

```bash
cd backend
npm run dev
```

Backend должен запуститься на `http://localhost:3001`

### 3. Создание тестового пользователя

```bash
cd backend
node create-test-user.js
```

Скрипт выведет JWT токен для авторизации.

### 4. Запуск frontend

```bash
npm run dev
```

Frontend должен запуститься на `http://localhost:8080`

---

## 🧪 Тестирование функциональности

### Шаг 1: Авторизация

1. Откройте `http://localhost:8080`
2. Откройте консоль разработчика (F12)
3. Выполните команду с токеном из шага 3:
   ```javascript
   localStorage.setItem("auth_token", "ВАШ_JWT_ТОКЕН_ЗДЕСЬ")
   ```
4. Обновите страницу (F5)

### Шаг 2: Генерация документа

1. Перейдите на страницу генератора
2. Введите тему документа
3. Пройдите все шаги генерации
4. На этапе титульного листа проверьте:
   - ✅ Кнопка "Предпросмотр" доступна
   - ✅ Можно просмотреть содержимое документа

### Шаг 3: Предпросмотр документа

1. Нажмите кнопку "Предпросмотр"
2. Проверьте модальное окно:
   - ✅ Титульный лист отображается
   - ✅ Все разделы с содержимым
   - ✅ Таблицы и графики (если есть)

### Шаг 4: Первое (бесплатное) скачивание

1. Заполните поля титульного листа
2. Нажмите "Скачать .docx"
3. Проверьте:
   - ✅ Документ скачивается
   - ✅ Токены сохраняются в БД
   - ✅ Бесплатное скачивание используется

### Шаг 5: Проверка неоплаченных токенов

1. Попробуйте скачать второй документ
2. Должна появиться ошибка о неоплаченных токенах
3. Проверьте через API:
   ```bash
   curl http://localhost:3001/api/tokens/unpaid \
     -H "Authorization: Bearer ВАШ_ТОКЕН"
   ```

### Шаг 6: Создание платежа за токены

1. Создайте платеж:
   ```bash
   curl -X POST http://localhost:3001/api/payments/create-tokens \
     -H "Authorization: Bearer ВАШ_ТОКЕН" \
     -H "Content-Type: application/json"
   ```
2. Получите ссылку на оплату
3. После оплаты (или в тестовом режиме YooKassa) токены должны быть помечены как оплаченные

### Шаг 7: Повторное скачивание

После оплаты токенов:
- ✅ Можно скачивать документы без ограничений
- ✅ Токены продолжают накапливаться

---

## 🔍 Проверка работы БД

### Проверка таблиц

Подключитесь к PostgreSQL и проверьте:

```sql
-- Проверка бесплатных скачиваний
SELECT * FROM free_downloads;

-- Проверка использования токенов
SELECT * FROM token_usage ORDER BY created_at DESC LIMIT 10;

-- Проверка подписок
SELECT * FROM subscriptions;
```

---

## 📊 Ожидаемое поведение

1. **Первое скачивание:**
   - ✅ Бесплатно
   - ✅ Токены сохраняются в БД (paid = false)
   - ✅ Создается запись в `free_downloads`

2. **Второе скачивание (без оплаты):**
   - ❌ Блокируется с ошибкой "unpaid_tokens"
   - Показывает стоимость неоплаченных токенов

3. **После оплаты токенов:**
   - ✅ Токены помечаются как оплаченные (paid = true)
   - ✅ Можно скачивать документы дальше
   - ✅ Новые токены накапливаются для следующей оплаты

---

## 🐛 Известные проблемы и решения

### Проблема: Backend не запускается

**Решение:** Проверьте `DATABASE_URL` в `backend/.env` - должна быть корректная строка подключения к PostgreSQL.

### Проблема: Ошибка "DATABASE_URL не задан"

**Решение:** Убедитесь, что `backend/.env` файл существует и содержит `DATABASE_URL`.

### Проблема: Токены не сохраняются

**Решение:** Проверьте консоль браузера - должны быть логи от `tokenTracker`. Также проверьте, что после скачивания вызывается `/api/tokens/record`.

---

## 📝 Чеклист тестирования

- [ ] Backend запущен на порту 3001
- [ ] Frontend запущен на порту 8080
- [ ] Тестовый пользователь создан
- [ ] Авторизация работает
- [ ] Генерация документа работает
- [ ] Предпросмотр документа работает
- [ ] Первое (бесплатное) скачивание работает
- [ ] Токены сохраняются в БД
- [ ] Второе скачивание блокируется (неоплаченные токены)
- [ ] API для получения неоплаченных токенов работает
- [ ] Создание платежа за токены работает
- [ ] После оплаты можно скачивать документы

---

## 🎉 Готово к тестированию!

Все компоненты реализованы и готовы к проверке.

# 🚀 Создание тестового пользователя через API

## ✅ Решение проблемы с подключением к БД

Поскольку внешнее подключение к Render PostgreSQL не работает с вашего компьютера, создан **API endpoint**, который можно вызвать прямо на сервере Render, где backend уже подключен к БД.

## 📋 Как создать тестового пользователя

### Вариант 1: Через curl или HTTP запрос

Вызовите endpoint на вашем сервере Render:

```bash
curl -X POST https://your-backend-url.onrender.com/api/admin/create-test-user \
  -H "Content-Type: application/json" \
  -d '{"secret": "your-admin-secret"}'
```

### Вариант 2: Добавить ADMIN_SECRET в переменные окружения Render

1. Зайдите в Render Dashboard
2. Откройте ваш **Backend сервис**
3. Перейдите в **Environment** или **Environment Variables**
4. Добавьте новую переменную:
   - **Key:** `ADMIN_SECRET`
   - **Value:** любой секретный ключ (например: `admin-secret-2025-yookassa`)
5. Сохраните изменения

### Вариант 3: Использовать дефолтный секрет (не рекомендуется для production)

Если `ADMIN_SECRET` не задан, endpoint использует `change-me-in-production` (но это небезопасно).

## 🔐 Защита endpoint

Endpoint защищен секретным ключом для безопасности. Убедитесь, что установили `ADMIN_SECRET` в переменных окружения Render.

## 📝 Пример ответа

После успешного создания вы получите:

```json
{
  "success": true,
  "user": {
    "id": 123,
    "username": "yookassa_test",
    "firstName": "Тестовый пользователь Юкасса"
  },
  "credentials": {
    "login": "yookassa_test",
    "password": "YooKassa2025!Test"
  },
  "token": "eyJhbGciOiJ...",
  "message": "Тестовый пользователь успешно создан"
}
```

## 🎯 После создания пользователя

1. Сохраните данные для входа:
   - Логин: `yookassa_test`
   - Пароль: `YooKassa2025!Test`

2. Отправьте данные Юкассе согласно инструкции в `YOOKASSA_ACCESS_INSTRUCTIONS.md`

## 🔄 Если нужно пересоздать пользователя

Просто вызовите endpoint еще раз - он обновит существующего пользователя или создаст нового.


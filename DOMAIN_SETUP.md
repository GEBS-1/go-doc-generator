# 🌐 Настройка домена для проекта

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка на Render

#### 1.1. Добавь домен в Static Site (Frontend)

1. Зайди в Render Dashboard → твой **Static Site** (`go-doc-generator`)
2. Открой **Settings** → **Custom Domains**
3. Нажми **Add Custom Domain**
4. Введи свой домен (например: `example.com` или `www.example.com`)
5. Render покажет DNS записи, которые нужно добавить

#### 1.2. Добавь домен в Web Service (Backend)

1. Зайди в Render Dashboard → твой **Web Service** (`go-doc-generator-backend`)
2. Открой **Settings** → **Custom Domains**
3. Нажми **Add Custom Domain**
4. Введи поддомен для backend (например: `api.example.com`)
5. Запиши DNS запись для backend

---

### Шаг 2: Настройка DNS у регистратора домена

#### 2.1. Определи, где находится домен

Обычно это:
- **Reg.ru**
- **Namecheap**
- **GoDaddy**
- **Cloudflare** (если используешь их DNS)
- Другой регистратор

#### 2.2. Добавь DNS записи

Зайди в панель управления доменом → **DNS настройки** → **Управление DNS записями**

**Для Frontend (Static Site):**
```
Тип: CNAME
Имя: @ (или www, или оставить пустым - зависит от провайдера)
Значение: <имя-твоего-статического-сайта>.onrender.com
TTL: 3600 (или Auto)
```

**Для Backend (Web Service):**
```
Тип: CNAME
Имя: api (или другое имя поддомена)
Значение: <имя-твоего-backend>.onrender.com
TTL: 3600
```

**Если Render требует A записи:**
```
Тип: A
Имя: @
Значение: <IP адрес от Render>
TTL: 3600
```

#### 2.3. Примеры DNS записей

**Для домена `example.com`:**

```
# Frontend
CNAME  @     →  go-doc-generator.onrender.com
CNAME  www   →  go-doc-generator.onrender.com

# Backend
CNAME  api   →  go-doc-generator-backend.onrender.com
```

---

### Шаг 3: Обновление переменных окружения

#### 3.1. На Render (Frontend Environment)

Добавь/обнови в **Environment** твоего **Static Site**:

```env
VITE_BACKEND_URL=https://api.example.com
VITE_TELEGRAM_BOT_USERNAME=твой_бот
VITE_REQUIRE_AUTH=true
```

#### 3.2. На Render (Backend Environment)

Добавь/обнови в **Environment** твоего **Web Service**:

```env
FRONTEND_URL=https://example.com
ALLOWED_ORIGINS=https://example.com,https://www.example.com
AUTH_JWT_SECRET=<твой-секрет>
TELEGRAM_BOT_TOKEN=<токен-бота>
GIGACHAT_AUTH_KEY=<ключ-гигачат>
# ... остальные переменные
```

**Важно:**
- В `ALLOWED_ORIGINS` укажи **оба варианта** домена (с `www` и без)
- Используй **https://** (не http://)
- Не добавляй слэш в конце (`example.com`, а не `example.com/`)

---

### Шаг 4: Ожидание распространения DNS

После добавления DNS записей:

1. **Подожди 5–30 минут** (иногда до 48 часов, но обычно быстро)
2. Проверь распространение DNS:
   - Открой https://dnschecker.org
   - Введи свой домен
   - Убедись, что записи появились по всему миру

3. **Проверь SSL сертификат:**
   - Render автоматически выпустит SSL сертификат (Let's Encrypt)
   - Это может занять до 1 часа после настройки DNS

---

### Шаг 5: Проверка работы

#### 5.1. Проверь Frontend

1. Открой свой домен в браузере: `https://example.com`
2. Должен открыться твой сайт (не страница Render)
3. Открой консоль браузера (F12) → **Network**
4. Убедись, что запросы идут на `https://api.example.com`

#### 5.2. Проверь Backend

1. Открой `https://api.example.com/health`
2. Должен вернуться JSON: `{"status":"ok",...}`

#### 5.3. Проверь авторизацию

1. Попробуй войти через Telegram
2. Убедись, что запросы проходят без CORS ошибок

---

### Шаг 6: Обновление Telegram Bot

Если у тебя есть Telegram бот, обнови домен в его настройках:

1. Зайди в [@BotFather](https://t.me/BotFather)
2. Отправь `/mybots`
3. Выбери своего бота
4. **Edit Bot** → **Edit Commands** или **Description**
5. Если есть ссылки, обнови их на новый домен

---

## 🔍 Решение проблем

### Проблема: Домен не открывается

**Решение:**
1. Проверь DNS записи на dnschecker.org
2. Убедись, что DNS записи правильные
3. Подожди еще 30 минут
4. Проверь в Render → **Custom Domains** → статус домена

### Проблема: SSL сертификат не выдается

**Решение:**
1. Подожди до 1 часа
2. Проверь, что DNS записи распространились
3. В Render → **Custom Domains** → нажми **Verify**
4. Если не работает, удали и добавь домен заново

### Проблема: CORS ошибки после смены домена

**Решение:**
1. Проверь `ALLOWED_ORIGINS` в backend Environment
2. Убедись, что указан **https://** (не http://)
3. Убедись, что нет слэша в конце
4. Перезапусти backend сервис на Render (Manual Deploy)

### Проблема: Фронтенд не может подключиться к backend

**Решение:**
1. Проверь `VITE_BACKEND_URL` в frontend Environment
2. Убедись, что указан поддомен backend (например: `https://api.example.com`)
3. Проверь, что backend доступен: `https://api.example.com/health`
4. Пересобери frontend (Manual Deploy)

---

## 📝 Пример полной конфигурации

### DNS записи (у регистратора домена):
```
CNAME  @     →  go-doc-generator.onrender.com
CNAME  www   →  go-doc-generator.onrender.com
CNAME  api   →  go-doc-generator-backend.onrender.com
```

### Frontend Environment (Render):
```env
VITE_BACKEND_URL=https://api.example.com
VITE_TELEGRAM_BOT_USERNAME=my_bot_name
VITE_REQUIRE_AUTH=true
```

### Backend Environment (Render):
```env
FRONTEND_URL=https://example.com
ALLOWED_ORIGINS=https://example.com,https://www.example.com
AUTH_JWT_SECRET=c9a13d9a299a6b78c2a51d30ddb865a6b787d02c9747bddfedd33617512313dd
TELEGRAM_BOT_TOKEN=123456789:ABCdefGhIJKlmNoPQRSTuvWXyz1234567890
GIGACHAT_AUTH_KEY=ZTZiMDFmNTItNGYyYS00NzAyLWE1ZDEtYjBjNmQ1YzNiMzg2OmNlYjU5NGVlLTE2OTktNGQyYy04YzE1LTBlNTBmOWI0MTU4Zg==
DATABASE_URL=postgres://user:password@host:5432/database
# ... остальные переменные
```

---

## ✅ Чек-лист

- [ ] Домен добавлен в Render (Static Site)
- [ ] Поддомен для backend добавлен в Render (Web Service)
- [ ] DNS записи добавлены у регистратора домена
- [ ] DNS записи распространились (проверено на dnschecker.org)
- [ ] SSL сертификат выдан (в Render → Custom Domains)
- [ ] `VITE_BACKEND_URL` обновлен на Render (Frontend)
- [ ] `FRONTEND_URL` обновлен на Render (Backend)
- [ ] `ALLOWED_ORIGINS` обновлен на Render (Backend)
- [ ] Frontend пересобран (Manual Deploy)
- [ ] Backend перезапущен (Manual Deploy)
- [ ] Домен открывается в браузере
- [ ] Backend доступен по поддомену
- [ ] Авторизация работает
- [ ] CORS ошибок нет

---

## 🎉 Готово!

После выполнения всех шагов твой сайт будет доступен по своему домену!

**Проверь:**
- Frontend: `https://example.com`
- Backend: `https://api.example.com/health`


# 🔌 Прямое подключение к GigaChat API (без прокси)

## ✅ Решение

Приложение настроено для **прямых запросов** к GigaChat API из браузера без использования прокси-серверов.

### Что изменилось:

1. **Убран Vite proxy** из `vite.config.ts`
2. **Прямые URL** в `src/lib/gigachat.ts`:
   - OAuth: `https://ngw.devices.sberbank.ru:9443/api/v2/oauth`
   - API: `https://gigachat.devices.sberbank.ru/api/v1/chat/completions`

## ⚠️ Важно: CORS политика

GigaChat API может блокировать запросы из браузера из-за **CORS (Cross-Origin Resource Sharing)** политики.

### Если возникает ошибка CORS:

```
Access to fetch at 'https://ngw.devices.sberbank.ru:9443/api/v2/oauth' 
from origin 'http://localhost:8080' has been blocked by CORS policy
```

Это означает, что **GigaChat API не разрешает** прямые запросы из браузера.

### Решения для CORS:

#### Вариант 1: Использовать браузерное расширение (только для разработки)
- Установите расширение CORS Unblock/Disable CORS
- ⚠️ **Небезопасно** - только для локальной разработки

#### Вариант 2: Запустить Chrome с отключенной CORS проверкой (только для разработки)
```bash
# Windows
chrome.exe --user-data-dir="C:/Chrome dev session" --disable-web-security --disable-features=VizDisplayCompositor

# Mac
open -na Google\ Chrome --args --user-data-dir=/tmp/chrome_dev_test --disable-web-security

# Linux
google-chrome --user-data-dir=/tmp/chrome_dev_test --disable-web-security
```
⚠️ **Небезопасно** - только для локальной разработки

#### Вариант 3: Использовать прокси (рекомендуется)
Если CORS блокирует запросы, верните Vite proxy (см. `SSL_CERT_FIX.md`)

## 🔍 Проверка работы

1. Откройте консоль браузера (F12)
2. Попробуйте создать документ
3. Проверьте логи:
   - ✅ Если видите успешные запросы - всё работает!
   - ❌ Если видите CORS ошибку - нужен прокси

## 📋 Преимущества прямого подключения:

- ✅ Проще конфигурация
- ✅ Нет дополнительных прокси-серверов
- ✅ Меньше точек отказа
- ✅ Работает везде, где есть интернет

## 📋 Недостатки:

- ❌ Может быть заблокировано CORS
- ❌ API ключи видны в коде браузера (но это нормально для публичных ключей)
- ❌ Нет кэширования на уровне прокси

---

**Последнее обновление:** 2025-01-27  
**Статус:** ✅ Настроено для прямых запросов

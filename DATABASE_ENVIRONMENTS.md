# 🗄️ Базы данных: Локальная vs Production

## ✅ Правильно: У вас ДВЕ разные базы данных

### 1. 📍 Локальная разработка (localhost)
**Используется:** Локальная PostgreSQL на вашем компьютере

**Файл:** `backend/.env` (локальный, НЕ коммитится в Git)

```env
DATABASE_URL=postgresql://Telebot:пароль@127.0.0.1:5432/docugen
```

**Для чего:**
- Локальное тестирование
- Разработка новых фич
- Отладка

---

### 2. 🌐 Production (Render.com)
**Используется:** PostgreSQL на Render.com

**Где настроено:** Render Dashboard → Backend Service → Environment Variables

```env
DATABASE_URL=<Internal Database URL из Render PostgreSQL>
PGSSLMODE=require
```

**Для чего:**
- Рабочая версия сайта
- Реальные пользователи
- Production данные

---

## 🔍 Как это работает

### Локально:
1. Backend читает `backend/.env`
2. Подключается к локальной PostgreSQL
3. Используется локальная БД `docugen`

### На Render:
1. Backend читает переменные окружения из Render Dashboard
2. Подключается к Render PostgreSQL (Internal Database URL)
3. Используется production БД

**⚠️ ВАЖНО:** Это РАЗНЫЕ базы данных!

---

## ✅ Что нужно сейчас (для локального тестирования)

**Используйте локальную БД:**

1. Убедитесь, что локальная PostgreSQL запущена
2. Проверьте `backend/.env` - должен быть локальный `DATABASE_URL`:
   ```env
   DATABASE_URL=postgresql://Telebot:пароль@127.0.0.1:5432/docugen
   ```

3. Протестируйте подключение:
   ```bash
   cd backend
   node test-db-connection.js
   ```

4. Если не работает - исправьте пароль/пользователя в `backend/.env`

---

## 🌐 Для production (Render)

**НЕ трогайте сейчас!** Это настроено отдельно в Render Dashboard:

1. Render Dashboard → Backend Service → Environment
2. Там указан `DATABASE_URL` от Render PostgreSQL
3. Это отдельная БД, не связанная с локальной

---

## 📝 Итого

- ✅ **Локально:** Используем `backend/.env` с локальной БД
- ✅ **Production:** Используем Environment Variables в Render с Render PostgreSQL
- ✅ **Это правильно:** Две разные БД для разных окружений

---

## 🔧 Если локальная БД не работает

Проблема с локальной БД (как сейчас):
- Проверьте пароль в `backend/.env`
- Проверьте, что PostgreSQL запущен
- Проверьте имя пользователя

**Это НЕ влияет на production!** Production использует свою БД на Render.

